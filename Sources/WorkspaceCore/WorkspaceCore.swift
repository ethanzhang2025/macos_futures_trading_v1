// WorkspaceCore · 工作区模板 + 自选管理
// WP-24 占位骨架 · 后续 WP-43（自选）+ WP-44（多窗口布局）+ WP-55（工作区模板）填充
// 职责：多分组自选（拖拽排序）+ 多窗口布局保存 + 多模板切换（盘前/盘中/盘后）+ CloudKit 预埋
// 合规提示：CloudKit 仅存非敏感（自选代码 / 布局偏好），详 StageA补遗 G1

import Foundation
import Shared

public enum WorkspaceCoreModule {
    public static let version = "0.1.0-skeleton"
}
