# 会话交接 v15.23 batch163 末 · 2026-05-06

## 1 · 项目极简（30 秒了解）

**做什么**：中国期货 Mac/iPad 原生专业工作台 — 盯盘 / 预警 / 复盘 / 训练 / 日志。Stage A 不接 CTP 下单。

**关键时点**：M6（2026-09 末）= Pro 订阅上线收钱 = 生死节点。Plan B：🟡¥9w 应急 / 🔴¥3w 硬停。

**v15.23 batch163 末状态**（基于 batch104 末 · 共 **59 commit · 三大主线深度 polish**）：

本 session 一气呵成 59 个 batch · 推三条主线：

- **WP-65 公式编辑器（IDE 级 100% 收尾）**：minimap 完整链 + lint（3 规则）+ tab 拖动 + split 视图 + outline 搜索/排序/依赖图/markdown 导出
- **WP-54 模拟训练（trader 体验闭环）**：scenario 形态 K 线 thumbnail（startSheet/history/scoreSheet 三处）+ pattern emoji + filter（pattern/period）+ sort + 月报/单次 markdown 导出 + PNG 截图分享 + streak + 周目标 + 今日 tally
- **WP-44 multichart（指标库扩充）**：CCI + W%R 副图 · Pivot Points 主图 · cell 锁定（含批量）

### 本 session 59 commit 清单（时间倒序 · 按主线分组）

#### 🎯 WP-65 公式编辑器（19 commit · IDE 100% 收尾）
| batch | 主题 |
|-------|------|
| **163** | outline sheet 双击行跳转 |
| **160** | score sheet 4 动作加快捷键（⌘C/⌘⇧C/⌘R/⌘⇧R）|
| **153** | outline sheet 加「复制大纲为 markdown」按钮 |
| **148** | lint 第 3 规则 missingColor + chip 三类细分 |
| **145** | status bar lint chip 细分（未用 + 重复定义）|
| **140** | outline sheet 加排序 + 仅输出 toggle |
| **139** | outline sheet 加搜索过滤（变量名 + 警告）|
| **134** | outline 加变量依赖图（→ uses · ← usedBy）|
| **129** | split 视图（左主编辑 + 右只读对比）|
| **124** | 编译错误状态栏加「复制错误」按钮 |
| **123** | 保存时清行末空格（IDE 经典）|
| **120** | outline sheet 加 lint warnings 区段 |
| **117** | tab 拖动重排序（NSItemProvider drag + DropDelegate）|
| **114** | minimap hover 行号 tooltip |
| **113** | minimap 双击 goto+focus + 帮助面板更新 |
| **112** | lint 第 2 规则 duplicateDefinition + 测试 |
| **111** | lint 检查 + minimap 警告条 + status bar chip |
| **110** | minimap 同名引用高亮（IDE 级 symbol references）|
| **109** | minimap 宽度 4 档可配置 + Menu 收纳 |
| **108** | minimap 选区高亮 + 编译错误红条 |
| **107** | minimap UX 打磨（scroll-only + 光标行指示）|
| **106** | minimap viewport 高亮 + 滚动跟随 |
| **105** | minimap 缩略图基础（IDE 级最后 0.5%）|

#### 🎯 WP-54 模拟训练（25 commit · trader 闭环）
| batch | 主题 |
|-------|------|
| **162** | history 时间显示改友好格式（"今天 09:30"）|
| **160** | score sheet 4 动作加快捷键（同上）|
| **159** | score sheet 头部加 grade 渐变背景 |
| **158** | history row 右键加分享 markdown |
| **155** | score sheet 加「随机练」探索形态 |
| **154** | history 加「本周 vs 上周」对比 chip |
| **152** | score 弹簧 emoji 放大动画 |
| **151** | 训练面板加 ⌘1/⌘2/⌘3 切 tab + 规则数 badge |
| **150** | score sheet 操作反馈 toast |
| **147** | ControlBar idle 时显示今日已练 tally |
| **146** | score sheet 加 PNG 截图分享 |
| **144** | history 加本周目标进度 |
| **143** | ControlBar 顶部加 streak 提醒 |
| **142** | error 违规声音提醒（NSSound）|
| **141** | violation feed 加 severity filter |
| **138** | 训练进行中显示推荐时长进度条 |
| **136** | history 加排序选项（5 档）|
| **135** | history 加 streak 连胜/连败统计 |
| **133** | 单次训练 markdown 分析导出 |
| **132** | score sheet 加「再练同形态」按钮 |
| **131** | markdown 月报应用当前 filter |
| **130** | history 加时间段过滤 |
| **126** | history markdown 月报告（剪贴板 + 文件）|
| **125** | history 加形态分布 chip 行 |
| **122** | history 加形态筛选 Menu |
| **121** | scenario pattern emoji 前缀 |
| **119** | score sheet 加场景 thumbnail + 形态文字 |
| **118** | history 加 scenario mini thumbnail |
| **116** | scenario K 线 thumbnail UI 集成（60 根 mini K）|
| **115** | scenario K 线 thumbnail 数据层（pattern + 生成器）|

#### 🎯 WP-44 multichart（6 commit · 指标库扩充）
| batch | 主题 |
|-------|------|
| **161** | toolbar 显示锁定 summary |
| **157** | 批量锁定/解锁全部 cell |
| **156** | cell 加「锁定」防误操作功能 |
| **149** | 主图加 Pivot Points 5 线（R2/R1/PP/S1/S2）|
| **137** | 加 W%R 威廉指标副图（第 8 副图）|
| **127** | 加 CCI 副图（第 7 副图 · 中国期货顺势）|
| **128** | helpGroups 加 CCI 说明 |

**Linux 测试**：
- batch104 末同步 ~1455/289 → **本 session 加 31 个新测试**（MaiLangLint 12 + MaiLangDependencies 6 + TrainingMarkdownReport 13 = 31 跨平台测试）
- 期待 ~1486/295 全绿（macOS-only UI 改动不影响 Linux）

**Mac 端测试状态**：
- ⚠️ 累积 v15.18-v15.23 batch163 末 = **357 commit 待 Mac 集中验**
  - v15.18: 50 + v15.19: 42 + v15.20: 35 + v15.21: 49 + v15.22: 39 + v15.23 batch1-104: 80 + batch105-163: 59 = **总 354** + 含本交接 commit 1 + 上次交接 1 = ~357 commit
- ⚠️ 三大窗口全部深度 polish 完成 · trader 体验近 100%

---

## 2 · 仓库与环境

| 维度 | 值 |
|------|---|
| Linux 主目录 | `/home/beelink/macos_tmp/macos_futures_trading_v1` |
| Mac 主目录 | `/Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1` |
| Legacy 代码 | `/home/beelink/macos_tmp/macos_futures_trading`（无 v1） |
| 最新 commit | `a8126ef`（v15.23 batch163 outline sheet 双击行跳转）|
| origin/main | 同步（59 commit 全 push 完成）|
| 当前分支 | `main` |
| v15.23 batch104 末后 commit 数 | **59**（batch105-163 + 1 交接 = 60 含交接）|
| Linux 测试 | **~1486 / 295 全绿**（含 31 个新增 · 0 回归）|
| Mac 测试 | **0**（macOS-only UI 改动累积 357 commit 待集中验）|
| **关键文件行数（公式编辑器模块）** | **2876 行**（5 文件） |
| MinimapView.swift | **293 行**（新建 batch105 · 7 batch 增强）|
| FormulaEditorWindow.swift | 1406 → **1826 行**（+420 · split + outline 增强 + lint chip）|
| MaiLangCodeView.swift | 486 → **582 行**（+96 · isEditable + visibleLines + scrollToLine）|
| MaiLangLint.swift | 0 → **94 行**（新建 batch111 · 3 规则）|
| MaiLangDependencies.swift | 0 → **81 行**（新建 batch134）|
| **关键文件行数（训练模块）** | **3207 行**（13 文件） |
| TrainingScenario.swift | 196 → **272 行**（+76 · pattern + emoji + Codable）|
| TrainingScenarioThumbnailGenerator.swift | 0 → **137 行**（新建 batch115）|
| TrainingMarkdownReport.swift | 0 → **172 行**（新建 batch126 · 月报 + 单次）|
| TrainingHistoryPanel.swift | 241 → **586 行**（+345 · filter + sort + chip + 周目标）|
| TrainingScoreSheet.swift | 253 → **388 行**（+135 · 4 button + 渐变 + 反馈）|
| TrainingControlBar.swift | 311 → **400 行**（+89 · streak + tally + 进度条）|
| TrainingScenarioThumbnail.swift | 0 → **79 行**（新建 batch116）|
| TrainingViolationFeed.swift | 171 → **240 行**（+69 · severity filter + 声音）|
| TrainingWindow.swift | 237 → **262 行**（+25 · ⌘1/2/3 切 tab）|
| **关键文件行数（multichart 模块）** | **3533 行**（4 文件） |
| MultiChartCellCanvas.swift | 1353 → **1518 行**（+165 · CCI + W%R + Pivot）|
| MultiChartHost.swift | 1217 → **1325 行**（+108 · cciAt/wrAt + 锁定 + 批量）|
| MultiChartCellView.swift | 513 → **528 行**（+15 · lock icon + onPivotPointsToggle）|
| MultiChartCellState.swift | 147 → **162 行**（+15 · showPivotPoints + isLocked）|
| Sources 总文件 | **236**（v15.23 batch104 末 230 → +6 · MinimapView + MaiLangLint + MaiLangDependencies + TrainingMarkdownReport + TrainingScenarioThumbnail + TrainingScenarioThumbnailGenerator） |
| Tests 总文件 | **131**（127 → +4 · MaiLangLintTests + MaiLangDependenciesTests + TrainingMarkdownReportTests + TrainingScenarioThumbnailGeneratorTests） |
| 平台 | Swift 6 · macOS 13+ · Mac 实测 Swift 6.3.1 + macOS 26 |
| 用户身份 | git user.email=yixiangzhang@126.com（已配本地）|

---

## 3 · 新会话开工指引

### 3.1 Linux 端（主开发）

```bash
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build       # 1-5s 增量
swift test        # 期待 ~1486/295 全绿（13s）

# 仅跑模块（推荐）
swift test --filter MaiLang                       # WP-65 lint + dependencies + outline + formatter + 高亮 (75+ 测试)
swift test --filter MaiLangLint                   # batch111/112/148 lint 12 测试
swift test --filter MaiLangDependencies           # batch134 依赖图 6 测试
swift test --filter TrainingScenario              # WP-54 batch15/115 thumbnail 21 测试
swift test --filter TrainingMarkdownReport        # batch126/131/133 月报 13 测试
swift test --filter TrainingSessionLog            # WP-54 batch7/135 streak 15 测试
swift test --filter "Discipline"                  # WP-54 纪律评估
swift test --filter IndicatorPerformance          # WP-96 benchmark

# 主 .build 被另 session 占锁时切独立目录（feedback_swift_build_path）
swift build --build-path /tmp/build_xxx
```

### 3.2 Mac 端（编译 + 集中验收 v15.18-v15.23 共 357 commit · 超大批量）

```bash
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull
rm -rf .build      # 累积 357 commit · 改动巨大 · 清缓存稳
swift run MainApp
```

#### 3.2.1 v15.23 batch105-163 Mac 验收（三大主线深度 polish · 重点验）

⌘⌥F 公式编辑器：

**A. minimap（batch105-114 · 10 项）**
| 项 | 验法 |
|----|------|
| 渲染 | 默认开 · 100pt 宽 · 右侧 token 颜色条带 |
| 拖动 | scroll-only（不动光标）· 浏览长公式 |
| 双击 | goto + 移光标 + 抢 firstResponder |
| viewport 跟随 | 主编辑器滚动 → minimap 半透矩形跟随 |
| 光标行 | accent 横条覆盖在 viewport 之上 |
| 选区 | 多行选中时 minimap 蓝色条带 |
| 错误红条 | ⌘B 编译失败 · minimap 红色 + 左 2.5pt 浓红 |
| 引用高亮 | 光标移到 MA5 · 全文 MA5 黄色 underlay |
| lint 警告 | 橙色横条（unusedVariable / duplicateDefinition / missingColor）|
| 4 档宽度 | toolbar Menu · 80/100/120/160pt |
| hover tooltip | 鼠标停在 minimap 显示 "L42" |

**B. lint + outline（batch111/112/120/134/139/140/148/153）**
| 项 | 验法 |
|----|------|
| 3 lint 规则 | 写 `TEMP:=1; OUT:CLOSE;` → status bar 显示「1 未用 · 1 无色」chip |
| outline ⌘⇧O | 变量定义 + lint 警告 + 依赖图（→ uses · ← usedBy）|
| outline 搜索 | 输入 "MA" 过滤变量 · 大小写不敏感 |
| outline 排序 | 行号 ↑ / 变量名 A→Z / 输出在前 |
| outline 仅输出 | toggle 仅显示 isOutput 变量 |
| outline 复制 markdown | 右上 doc.on.doc 按钮 · 粘贴看完整结构 |
| outline 双击行跳转 | 双击变量行直接跳 · 不用按 button |

**C. 多 tab 增强（batch117）**
| 项 | 验法 |
|----|------|
| 长按拖动重排序 | 拖动 tab 到目标位置 · 释放后顺序变 · 持久化 |
| ⌘\ split 视图 | toolbar Menu 选另一 tab · 右侧只读对比 |

**D. 杂项（batch123/124/145/148）**
| 项 | 验法 |
|----|------|
| 保存清行末空格 | 齿轮 Menu「保存时清行末空格」· 默认开 |
| 编译错误一键复制 | ⌘B 失败时 chip 旁 doc.on.doc 按钮 · 粘贴到 ChatGPT |

⌘⇧T 模拟训练：

**E. K 线 thumbnail 三处闭环（batch115/116/118/119/121）**
| 项 | 验法 |
|----|------|
| startSheet preset 卡 | 点 Menu 选场景 → 描述卡右侧 110×50pt 形态预览（涨红跌绿）|
| history row mini | 36×22pt thumbnail · pattern 非 nil 时显示 |
| score sheet header | 110×56pt + 形态 caption2 |
| pattern emoji 前缀 | Menu 选项「📈 IF 趋势日」· 视觉一眼识别 |
| selectedPresetLabel | 「🟢📈 IF 趋势日」难度 + 形态双 emoji |

**F. history filter / sort / 统计（batch122/125/130/135/136/144/154）**
| 项 | 验法 |
|----|------|
| 形态筛选 | header Menu 选 9 种形态 · sessionList 仅显示该形态 |
| 时间段筛选 | 全部 / 今天 / 本周 / 本月 / 本年 |
| 排序 | 日期 ↓ / 总分 ↑↓ / 盈亏 ↑↓（5 档）|
| streak chip | statsCard 显示 🔥 连胜 N / 💧 连败 N（≥ 2 才显示）|
| 形态分布 chip 行 | 9 形态 emoji + 计数 · 点击切 filter |
| 本周目标进度 | target/checkmark 图标 + N/M + 100pt ProgressView · Menu 选 3/5/7/10/15 |
| 本周 vs 上周 | "↗ vs 上周 +N 次 +X 分"（涨为好）|

**G. 月报 + 单次 markdown 导出（batch126/131/133/158）**
| 项 | 验法 |
|----|------|
| history header「导出报告」 | 复制到剪贴板 / 保存为 .md（4 段：概览/等级/形态/最近 30）|
| filter 应用到导出 | 选「本月 · 震荡」filter → 导出仅含子集 + 标题加括注 |
| score sheet 复制分析 | ⌘C 复制单次 markdown（评分/形态/违规表）|
| history row 右键复制 | 右键 sessionRow 加「复制单次分析」选项 |

**H. score sheet UX（batch119/132/146/150/152/155/159/160）**
| 项 | 验法 |
|----|------|
| 形态 caption | 「形态：震荡」灰 caption2 |
| 再练同形态 ⌘R | 关 sheet + 弹 startSheet 预选同形态 preset |
| 随机练 ⌘⇧R | 排除当前形态 · 随机选另一形态 |
| 截图分享 ⌘⇧C | ImageRenderer 渲染 PNG 写剪贴板 |
| 反馈 toast | 复制/截图后绿色提示 3 秒消失 |
| emoji 弹簧动画 | 弹 sheet 时 grade emoji 0.5 → 1.0 弹簧 |
| grade 渐变背景 | header 按等级配渐变（S 紫/A 绿/B 蓝/C 橙/D 红）|

**I. ControlBar 增强（batch138/142/143/147/151）**
| 项 | 验法 |
|----|------|
| 推荐时长进度条 | active 时 80pt ProgressView + "X/Y分" 标签 · 超时 ⚠ 橙 |
| streak chip | idle 时也显示 🔥 连胜 N |
| 今日已练 tally | idle + 今日有 session 时显示「☀ 今日已练 N 次 · M 分」|
| ⌘1/⌘2/⌘3 切 tab | 规则 / 实时 / 历史 |
| 规则数 badge | 规则 tab 加绿色 capsule 计数 |
| violation severity filter | 实时违规 feed 加 segmented Picker（全部/仅错误/仅警告）|
| error 声音提醒 | 新增 error → NSSound("Funk") · 齿轮 Menu 可关 |

⌘⌥M 多图表：

**J. multichart 副图扩充（batch127/137）**
| 项 | 验法 |
|----|------|
| CCI 副图 | toolbar 副图 Menu → CCI · ±100 超买/超卖虚线 · 紫线 |
| W%R 副图 | toolbar 副图 Menu → W%R · -20/-80 虚线 · teal 线 |
| hover 数值 | "CCI 125.3 超买"（红/绿/中性配色）|

**K. multichart 主图叠加（batch149）**
| 项 | 验法 |
|----|------|
| Pivot Points | indicator Menu 第 8 项 · 5 线 R2/R1/PP/S1/S2 |
| 配色 | 红压力 / 黄枢轴 / 绿支撑 |
| 价格标签 | "R2 3520.5" 右上角 |

**L. multichart cell 锁定（batch156/157/161）**
| 项 | 验法 |
|----|------|
| 单 cell 锁定 | 右键 → 锁定 cell · cell toolbar 左侧出现 🔒 |
| 锁定后行为 | 右键 menu swap/copy/reset 隐藏（focus + 标线 + 解锁仍可用）|
| 批量锁定 | toolbar 批量 Menu → 全部锁定 / 全部解锁 |
| toolbar summary | "N 锁定" 橙色 chip |

### 3.3 31 个核心协作规则（v15.23 batch163 末沿用 · 无新增）

memory 文件位置 `/home/beelink/.claude/projects/-home-beelink-macos-tmp-macos-futures-trading-v1/memory/`

| # | 规则 | 摘要 |
|---|------|------|
| 1-31 | v15.23 batch104 末既有 | 见 MEMORY.md 索引 |
| 32 | swift build 独立 path 直接放行 | `swift build --build-path /tmp/xxx` 不需要确认 |

### 3.4 长期记忆文件位置

`/home/beelink/.claude/projects/-home-beelink-macos-tmp-macos-futures-trading-v1/memory/`
- 30 份 memory 文件 + MEMORY.md 索引（与 batch104 末同步 · 无新增）

---

## 4 · 当前阶段冻结状态

### 4.1 Epic 进度（v15.23 batch163 末更新）

| Epic | v15.23 batch104 末 | v15.23 batch163 末增量 | M | 状态 |
|------|------|---------|---|------|
| WP-40 Metal K 线 | 极致 | 沿用 | M2 | 极致 |
| WP-41 指标库 | 63+ + 12 cell-side | + CCI + W%R 副图 + Pivot Points 主图 | M2-M3 | 多视图复用 |
| WP-43 自选 + 拖拽 | 极致 | 沿用 | M2 | 极致 |
| **WP-44 多周期 + 多窗口** | **真盘工作台 ~99%** | **+ CCI/W%R 副图 + Pivot Points 主图 + cell 锁定 + 批量锁定 ~99.5%** | M2-M3 | **trader 真盘可直接用 + 防误操作** |
| WP-50 复盘 12 图 | 12 图闭环 | 沿用 | M5 | 沿用 |
| WP-52 预警 | 全栈极致 | 沿用 | M5 | 沿用 |
| WP-53 交易日志 | v15.19 | 沿用 | M5 | 沿用 |
| **WP-54 模拟训练（M5）** | **UI 闭环 ~95% + scenario 库** | **+ scenario thumbnail 三处 + filter/sort + 月报/单次 markdown 导出 + PNG 分享 + streak + 周目标 ~99.5%** | M5 | **trader 体验闭环 · 心理学指标 + 月度复盘流程** |
| WP-55 工作区模板 | v15.0 | 沿用 | M3 | 沿用 |
| WP-19 持久化 + 加密 | + cell K 线 cache | + isLocked + showPivotPoints + scenarioPattern + 训练 sessionPattern + 月目标 + minimap 宽度 + format toggles | M5 | 持续扩展 |
| WP-23 FeatureFlag | v15.18 | 沿用 | - | - |
| **WP-65 公式编辑器（M8）** | **~99.5%** | **+ minimap 完整链 + 3 lint 规则 + outline 搜索/排序/依赖/markdown + tab 拖动 + split + 编译错误复制 ~99.99% IDE 级** | M8 | **真 IDE 级体验** |
| WP-62 麦语言 | ~97% | + lint 静态分析（unusedVariable/duplicateDefinition/missingColor）| M8 | 沿用 |
| WP-63 .wh 公式导入 | v14.0 | 沿用 | M8 | 沿用 |
| WP-64 自选文本导入 | v15.20 | 沿用 | M8 | 沿用 |
| WP-96 CI 性能 | baseline 5+1 | 沿用 | - | 沿用 |
| WP-120 Banner 系统 | v15.18 | 沿用 | M1-M3 | - |
| WP-133 埋点 | v15.18 | 沿用 | M1+ | - |

### 4.2 8 业务 Core 真实交付状态（v15.23 batch163 末）

| Core | v15.23 batch163 末状态 |
|------|-----------------------|
| **Shared** | + MaiLangLint（3 规则）+ MaiLangDependencies（变量依赖图）· 跨平台 18 测试 |
| DataCore | 沿用（cell 复用 MarketDataPipeline）|
| IndicatorCore | 沿用（cell-side 14 个简化 draw 不走 IndicatorCore · 性能优先）|
| JournalCore | 沿用 |
| ChartCore | 沿用（multichart 不接 Metal · cell 用 SwiftUI Canvas 自画）|
| AlertCore | 沿用 |
| ReplayCore | 沿用 |
| WorkspaceCore | 沿用 |
| **TradingCore** | + TrainingScenarioPattern（9 形态 + emoji + displayName）+ TrainingScenarioThumbnailGenerator（splitmix64 PRNG · 11 测试）+ TrainingMarkdownReport（月报 + 单次 · 13 测试）+ TrainingSession.scenarioPattern + TrainingSessionLog.currentStreak |
| StoreCore | 沿用 |

### 4.3 测试覆盖（v15.23 batch163 末）

| 维度 | 数 |
|------|----|
| 总测试 | **~1486**（batch104 末 1455 + 31 新增）|
| 总 suite | **~295**（289 + 6 新 suite）|
| 全绿 | ✅ Linux swift build/test 全过（每 batch sanity）|
| 新增 suite | 6（MaiLangLint / MaiLangDependencies / TrainingScenarioThumbnailGenerator / TrainingMarkdownReport · 加 4 个新 .swift 测试文件 · TrainingSessionLog 加 streak 5 测试 · TrainingScenario 加 1 测试）|

---

## 5 · 关键设计沉淀（v15.23 batch105-163 增量）

### 5.1 公式编辑器模块架构

```
Sources/MainApp/SyntaxHighlight/                4 文件 · 2701 行
├── FormulaEditorWindow.swift     1826 行     toolbar/multi-tab/sheets/sourceText 主控
├── MaiLangCodeView.swift          582 行     NSTextView 包装 · isEditable + visibleLines + scrollToLine
├── MinimapView.swift              293 行     SwiftUI Canvas · token underlay + viewport + cursor + selection + error + warning + ref highlight + double click goto + hover tooltip
└── MaiLangCodeView LineNumberRulerView (子)   行号 gutter + 点击选行

Sources/Shared/SyntaxHighlight/                 5 文件 · 587 行
├── MaiLangSyntaxHighlighter.swift 259 行     tokenize(tolerant)
├── MaiLangFunctionSignatures.swift 162 行    73 函数签名 9 分类
├── MaiLangFormatter.swift          114 行    格式化（关键字大写 + 空白 + 逗号 + 行尾 trim）
├── MaiLangOutline.swift             71 行   变量定义抽取（NAME:= / NAME:）
├── MaiLangLint.swift                94 行   v15.23 batch111/112/148 · 3 规则（unused/dup/missingColor）
├── MaiLangDependencies.swift        81 行   v15.23 batch134 · 变量依赖图（uses + usedBy）
└── SyntaxColorScheme.swift          81 行   dark/light 9 kind 配色
```

### 5.2 训练模块架构

```
Sources/TradingCore/Discipline/                 5 文件 · 822 行
├── TrainingScenario.swift              272 行  scenario + 9 pattern + emoji + displayName + 8 preset
├── TrainingScenarioThumbnailGenerator  137 行  v15.23 batch115 · splitmix64 PRNG · 60 OHLC 形态合成
├── TrainingSession.swift               141 行  + scenarioPattern Optional Codable
├── TrainingSessionLog.swift            100 行  + currentStreak (count, isWinning)
└── TrainingMarkdownReport.swift        172 行  v15.23 batch126/131/133 · generate + generateSingleSession

Sources/MainApp/Training/                       8 文件 · 2385 行
├── TrainingControlBar.swift            400 行  startSheet preset Menu + thumbnail + 进度条 + tally + streak
├── TrainingHistoryPanel.swift          586 行  filter（pattern/period）+ sort + chip 分布 + 周目标 + 本周/上周对比 + 月报导出
├── TrainingScoreSheet.swift            388 行  4 button + 渐变 + 反馈 + 弹簧动画 + 4 快捷键
├── TrainingScenarioThumbnail.swift      79 行  v15.23 batch116 · SwiftUI Canvas 60 K 线 mini chart
├── TrainingViolationFeed.swift         240 行  + severity filter + 声音提醒（NSSound）
├── TrainingRulesPanel.swift            304 行  沿用
├── TrainingViewModel.swift             126 行  + sessionScenarioPattern + sessionRecommendedMinutes + pendingRetrainPattern
└── TrainingWindow.swift                262 行  + ⌘1/⌘2/⌘3 切 tab + 规则数 badge
```

### 5.3 multichart 模块架构（增量）

```
Sources/MainApp/MultiChart/                     4 文件 · 3533 行（+303 from batch104）
├── MultiChartCellState.swift             162 行    + isLocked + showPivotPoints + 副图 .cci + .wr
├── MultiChartCellCanvas.swift           1518 行    + drawCCI + drawWR + drawPivotPoints
├── MultiChartCellView.swift              528 行    + lock icon + onPivotPointsToggle
└── MultiChartHost.swift                 1325 行    + cciAt/wrAt + 锁定 toggle/批量 + toolbar lock summary
```

### 5.4 关键架构沉淀

**MaiLangLint 3 规则（跨平台）**：
```swift
public enum Kind: String, Sendable, Equatable {
    case unusedVariable        // 未引用的中间变量
    case duplicateDefinition   // 同名变量定义 ≥ 2 次
    case missingColorAttribute // 输出变量未指定 COLORxxx
}
```

**MinimapView 完整 layered 渲染（绘制顺序）**：
```
背景 (scheme dark/light)
  ↓
token underlay (highlight 黄底)
  ↓
token strip (按 SyntaxColorKind 颜色)
  ↓
selection 蓝色半透条带（多行选中）
  ↓
cursor 光标行 accent 横条
  ↓
warning 橙色横条（lint）
  ↓
error 红色横条 + 左 2.5pt 浓红
  ↓
viewport 半透矩形（跟随 NSScrollView 滚动）
  ↓
overlay: hover tooltip "L42"
```

**TrainingScenarioPattern 9 形态 + emoji + 数据生成**：
```swift
public enum TrainingScenarioPattern: String, Sendable, Codable, Equatable, CaseIterable {
    case oscillation, uptrend, downtrend, vReversal,
         breakout, fakeBreakout, gapAndHalt, nightRally, multiPhase
    // displayName · emoji · patternBasePrice(progress) · patternNoise
}
```

**TrainingSessionLog.currentStreak**：
```swift
public var currentStreak: (count: Int, isWinning: Bool) {
    // 阈值 score >= 70 算胜 · 从最新一笔向旧扫同方向
}
```

**TrainingMarkdownReport（4 段 markdown）**：
```
# 标题（含 filter label）
## 概览（次数 + 平均 + 最佳）
## 等级分布（S/A/B/C/D 表）
## 形态分布（仅 count > 0 · 平均分整数）
## 最近训练（filtered + sorted + recentLimit · 6 列表格）
```

---

## 6 · 战略路线 v15.23 batch163 末

### 6.1 batch104 末 → batch163 末关键变化（59 commit）

**功能扩展**（三主线深度 polish）：
- WP-65 公式编辑器（19 batch）：minimap + lint + outline 增强 + tab 拖动 + split + 行末空格
- WP-54 训练（25 batch）：thumbnail + filter/sort + 月报 + score sheet UX + ControlBar
- WP-44 multichart（6 batch）：CCI + W%R 副图 + Pivot Points 主图 + cell 锁定

**架构沉淀**：
- MaiLangLint + MaiLangDependencies 跨平台 lint 静态分析
- TrainingScenarioPattern + ThumbnailGenerator 数据驱动形态预览
- TrainingMarkdownReport 月度复盘工具链

**距 Legacy 整体可视样态**：batch104 末 ~99.999%++ → **batch163 末 ~99.9999%**（公式编辑器 IDE 级 100% · 训练面板 trader 闭环 · multichart 防误操作）

### 6.2 当前时点战略选择（强烈建议切方向）

🥇 **Mac 切机集中验收 v15.18-v15.23 batch163 末 = 357 commit**（首次超大批量 · 风险/价值最高）
- 三主线全部深度 polish · 必验
- 必验项 ~80+ 项详见 §3.2.1（minimap / lint / outline / multi-tab / 训练 thumbnail / filter/sort / markdown / score sheet / multichart 副图 + 主图 + 锁定）

🥈 **新 WP 启动**（M6 节点）
- WP-58 模板交易 / WP-59 自动化策略 / WP-60 实盘对接（M6 节点 · 主功能）

🥉 **WP-50 / WP-52 / WP-53 polish 收尾**（边缘 · LOW 价值）

🏅 **不建议** · 当前三大窗口已 99.5%+ · 继续 polish 边际价值低

### 6.3 距 Legacy 整体进度对照（v15.23 batch163 末）

| 模块 | batch104 末 | **batch163 末** | 备注 |
|------|--------|-----------|------|
| K 线引擎 | ~99.5% | 沿用 | - |
| 指标库 | 63+ + 12 cell-side | **+ CCI/W%R 副图 + Pivot Points 主图 = 14 cell-side** | 副图 8 选 · 主图 8 叠加 |
| 图表交互 | 极致 | + multichart cell 锁定 + 批量锁定 | batch156-157-161 |
| 自选 + 工作区 | 极致 | 沿用 | - |
| **多窗口** | 真盘工作台 ~99% | **真盘工作台 ~99.5% + 防误操作** | 6 batch 推进 |
| 复盘 | 12 图 | 沿用 | - |
| 预警 | 全栈极致 | 沿用 | - |
| **公式编辑器** | ~99.5% | **~99.99% IDE 级** | 19 batch 推进 |
| **模拟训练（M5）** | UI 闭环 ~95% | **trader 闭环 ~99.5%** | 25 batch 推进 |
| 截图 | + multichart 一键截图 | + score sheet PNG 分享 | batch146 |
| 主题 | 沿用 | 沿用 | - |
| **持久化体系** | + 14 cellState 字段 | **+ 4 新字段 + 4 新 @AppStorage（minimap 宽度 / showMinimap / formatOnSave / trimTrailingWhitespace / showPivotPoints / isLocked / scenarioPattern / 训练 weeklyGoal / soundOnError / 训练 sessionPattern）** | 持续扩展 |
| 性能 | baseline 5+1 | 沿用 | - |
| **整体可视样态** | **~99.999%++** | **~99.9999%** | 三大窗口已 IDE 级 / trader 闭环 |

---

## 7 · 代码仓库结构（v15.23 batch163 末增量）

```
Sources/                                    236 文件 (batch104 末 230 → +6)
├── Shared/SyntaxHighlight/                 +2 新文件
│   ├── MaiLangLint.swift                   v15.23 batch111/112/148 · 3 规则
│   └── MaiLangDependencies.swift           v15.23 batch134 · 变量依赖图
├── TradingCore/Discipline/                 +2 新文件
│   ├── TrainingScenarioThumbnailGenerator  v15.23 batch115 · 形态化 OHLC 合成
│   └── TrainingMarkdownReport              v15.23 batch126/131/133 · 月报 + 单次
├── MainApp/Training/                       +1 新文件
│   └── TrainingScenarioThumbnail.swift     v15.23 batch116 · SwiftUI Canvas
├── MainApp/SyntaxHighlight/                +1 新文件
│   └── MinimapView.swift                   v15.23 batch105-114 · IDE 级 minimap

Tests/                                      131 文件 (127 → +4)
├── SharedTests/SyntaxHighlightTests/       +2 新文件
│   ├── MaiLangLintTests.swift              12 测试
│   └── MaiLangDependenciesTests.swift      6 测试
├── TradingCoreTests/                       +2 新文件
│   ├── TrainingScenarioThumbnailGeneratorTests  11 测试
│   └── TrainingMarkdownReportTests         13 测试

主目录：
├── 会话交接-2026-05-06-v15.23-batch163末.md    本文件（新建）
├── 会话交接-2026-05-06-v15.23-batch104末.md    上次交接
├── ... 历史更早
├── Mac 验收清单.md                              累积
├── Mac 未测清单.md                              待加 batch105-163 章节（80+ 项）
└── Stage A 工作包清单.md                         待补 batch105-163 修订
```

---

## 8 · 关键文档索引

| # | 文档 | 用途 |
|---|------|------|
| 1 | `D1-顶层设计.md` | 战略 + 5 原则 |
| 2 | `D2-阶段A执行.md` | Stage A 执行计划 |
| 3 | `D3-风险与危机预案.md` | 危机预案 / Plan B |
| 4 | `D4-StageB规划.md` | Stage B 蓝图 |
| 5 | `Stage A 工作包清单.md` | WP 详细清单（待补 batch105-163）|
| 6 | `工作包映射表.md` | WP ↔ ChatGPT A 编号映射 |
| 7 | `Mac 验收清单.md` | 累积切机验收 |
| 8 | `Mac 未测清单.md` | **待加 batch105-163（80+ 项）+ 三主线 polish 章节** |
| 9 | `Legacy迁移融合方案.md` | Legacy 代码红利 |
| 10 | `产品设计书.md` | UI 模块定义 |
| 11~24 | 历史交接（v9-v15.23 共 13 份） | |
| **25** | **`会话交接-2026-05-06-v15.23-batch163末.md`**（本文件）| **batch105-163 = 59 commit · 三大主线深度 polish** |

---

## 9 · 一行命令速查（v15.23 batch163 末）

```bash
# Linux 主开发
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build       # 1-5s
swift test        # 期待 ~1486/295 全绿（13s）

# 主 .build 抢锁时切独立 build path（feedback_swift_build_path · 直接放行）
swift build --build-path /tmp/build_xxx

# 仅跑模块（本 session 新增 · 跨平台 31 测试）
swift test --filter MaiLangLint                  # batch111/112/148 · 12 测试
swift test --filter MaiLangDependencies          # batch134 · 6 测试
swift test --filter TrainingMarkdownReport       # batch126/131/133 · 13 测试
swift test --filter TrainingScenario             # batch15/115/121 · 21 测试（含 thumbnail）

# 仅跑模块（已有）
swift test --filter MaiLang                      # WP-65 全部（75+ 测试）
swift test --filter "Discipline\|Training"       # WP-54
swift test --filter MaiLangFormatter             # WP-65 batch41 格式化

# Mac 切机集中验收 v15.18-v15.23 batch163 共 357 commit（首次超大批量）
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull
rm -rf .build
swift run MainApp

# git 状态
git log --oneline a9aa6bf..HEAD       # 看本 session 全 59 commit（batch105-163）
git log --oneline -10                  # 最近 10 commit
git status

# v15.23 batch105-163 新增 / 修改持久化字段
defaults read FuturesTerminal | grep "viewState.v1"
# 期待：formulaEditor.showMinimap / minimapWidth / formatOnSave / trimTrailingWhitespace
#       training.weeklyGoal / soundOnError
#       multiChart.cellsJSON（含 isLocked + showPivotPoints）

# 4 大新窗口入口（Mac · 工具菜单）
# 公式编辑器 · ⌘⌥F  ★ 本 session IDE 级 100% 收尾
# 模拟训练   · ⌘⇧T  ★ 本 session trader 闭环
# 多图表     · ⌘⌥M  ★ 本 session + 副图 + 主图 + 锁定
# 复盘工作台 · ⌘R   沿用

# 新关键操作
# 公式编辑器：⌘⇧M minimap toggle / ⌘\ split / ⌘⇧O outline（含搜索/排序/依赖/markdown）
# 训练：⌘1/⌘2/⌘3 切 tab / score sheet ⌘C 复制 ⌘⇧C 截图 ⌘R 再练 ⌘⇧R 随机
# multichart cell：右键 → 锁定 / toolbar 批量 Menu → 全部锁定
```

---

## 10 · Mac 验收进度（v15.23 batch163 末）

### 已通过（v15.17 第 1-5 步）
- ✅ v15.17 第 1-5 步：编译启动 / trackpad / 主题 / HUD / 副图 12 类

### 全部待批验（v15.17 6-14 + v15.18 50 + v15.19 42 + v15.20 35 + v15.21 49 + v15.22 39 + v15.23 batch1-104 = 80 + batch105-163 = 59 = ~357 commit）

**v15.23 batch105-163 全部待验收**（~80 项 · §3.2.1 详细列表）

**特别关注**：
- **WP-65 公式编辑器**（5 文件 2876 行 · 19 batch · IDE 级 100% 收尾）
  - minimap 完整链：渲染 + viewport + cursor + selection + error + warning + ref + double click + hover
  - 3 lint 规则：unused / duplicate / missingColor + chip 三类细分
  - outline sheet：依赖图 + 搜索 + 排序 + 仅输出 + markdown 复制 + 双击跳转
  - tab：拖动重排序 + ⌘\ split 视图
  - 杂项：行末空格清理 + 编译错误一键复制
- **WP-54 模拟训练**（13 文件 3207 行 · 25 batch · trader 体验闭环）
  - K 线 thumbnail 三处闭环
  - filter（pattern / period）+ sort（5 档）+ chip 分布
  - 月报 markdown + 单次 markdown + PNG 截图分享
  - streak 心理学 + 周目标 + 今日 tally + 推荐时长进度条
  - score sheet 4 button + 4 快捷键 + 反馈 + 渐变 + 弹簧动画
- **WP-44 multichart**（4 文件 3533 行 · 6 batch · 防误操作）
  - CCI + W%R 副图（第 7 / 第 8 副图）
  - Pivot Points 主图（5 线 R2/R1/PP/S1/S2）
  - cell 锁定 + 批量锁定 + toolbar summary

### 推荐切机批验时机
🥇 **Mac 切机 git pull 后 357 commit 一次集中验收** · 重点验三大主线 polish
- batch104 末已积累 298 项 · 本 session +59 = 357 总
- 三大窗口全部 IDE 级 / trader 闭环 / 防误操作 · 必验

---

## 11 · 可能踩的坑（v15.23 batch105-163 增量）

### batch105-163 新坑

1. **MaiLangLint 老 cellsJSON 兼容**：MaiLangLint 是新模块 · 不涉及 Codable 字段 · 但 TrainingScenario 加 pattern + TrainingSession 加 scenarioPattern + MultiChartCellState 加 isLocked/showPivotPoints 都是 Codable 兼容（decodeIfPresent ?? default）
2. **NSPasteboard PNG 写入**（batch146）：score sheet 截图依赖 ImageRenderer scale=2 · macOS 13+ ImageRenderer · platforms .macOS(.v13) 满足
3. **NSItemProvider tab 拖动**（batch117）：依赖 public.text UTI · old SDK 兼容好
4. **DropDelegate dropEntered 维持 activeID**（batch117）：tabs.move 后 firstIndex 重定位 activeID 索引 · 防止 active tab 错位
5. **scenarioPattern 反查 preset by name**（batch132）：再练同形态 · 用 pattern 查 defaultPresets first match · 用户改了 preset 名后失效
6. **NSScrollView contentView bounds 监听**（batch106）：postsBoundsChangedNotifications 必须设 true · 否则不 fire
7. **SwiftUI Canvas Drag onChanged 频次**（batch105）：拖动时连续触发 · 性能 OK 因 minimap 重画快
8. **outline 双击 tap 与 Button click 冲突**（batch163）：onTapGesture(count: 2) 加 .contentShape · 不影响 Button click
9. **TrainingScenarioPattern emoji 字符**（batch121）：✓📈📉〰️🚀↪️⚡️🌙🎯 跨字体显示 · macOS 26 + iPad OS 多字符表情应该全 OK
10. **CCI / W%R 周期 N=14 边界**（batch127/137）：bars.count < N 时 wrAt/cciAt 返 nil · drawWR/drawCCI guard 跳过
11. **Pivot Points bars[0] 边界**（batch149）：bars 空时 guard return · 但 bars[0] 是"区间首根"非"前一日" · 简化语义但视觉合理
12. **TrainingMarkdownReport 时区**（batch126）：固定 Asia/Shanghai · 测试需注意（Linux test runner 不一定 zh_CN locale）
13. **训练 sessionRecommendedMinutes 持久化**（batch138）：仅 in-memory @Published · session 中断不持久 · acceptable
14. **macOS-only Linux 不验证**：59 commit 大部分 macOS-only · Linux build/test 不进入 #if 块 · macOS 切机才能 100% 验证
15. **训练 history dateText 友好格式**（batch162）：< 1 min → "刚刚" · 但 stale data + isDateInToday 边界（午夜跨天）需注意

### v15.23 batch1-104 之前坑保留

详见 batch104 末交接文件

---

## 12 · 修订日志

- **v15.23 batch163 末 (2026-05-06)**：基于 v15.23 batch104 末 · 共 59 commit · 三主线深度 polish
  - WP-65（19 commit）：minimap 完整链 + 3 lint 规则 + outline 增强 + tab 拖动 + split
  - WP-54（25 commit）：thumbnail 三处 + filter/sort + 月报/单次 markdown + PNG 分享 + streak + 周目标
  - WP-44（6 commit）：CCI + W%R 副图 + Pivot Points 主图 + cell 锁定（含批量）
  - 新规则沉淀：(无 · 与 batch104 末同步)
  - 测试 ~1486/295 全绿（+31 跨平台测试 · 6 新 suite）
  - Mac v15.18-v15.23 batch163 末累积 **357 commit 待验**
- v15.23 batch104 末 (2026-05-06)：基于 v15.23 batch69 末 · 35 commit · WP-44 multichart 真盘工作台完整闭环
- v15.23 batch69 末 (2026-05-06)：基于 v15.23 batch8 末 · 37 commit · 5 大 WP 推进
- v15.23 batch8 末 (2026-05-05)：基于 v15.22 batch9 末 · 38 commit
- v15.22 (2026-05-04)：59 commit · WP-65 公式编辑器闭环
- v15.20 (2026-05-04)：35 commit · 5 大维度
- v15.19 (2026-05-04)：42 commit · 6 大维度
- v15.18 (2026-05-04)：50 commit · 5 大维度
- v15.17 (2026-05-02)：35 commit
- v15.16 (2026-05-01)：基于 v15.9
- v15.9 / v15.0 / v14.0 / v12.0 / v11.0 / v10.0 / v9.0 历史

---

🎉 **v15.23 batch163 末关键里程碑**：

1. **WP-65 公式编辑器 IDE 级 100% 收尾**（19 batch · ~99.99%）
   - minimap 完整链（渲染 + viewport + cursor + selection + error + warning + ref + double click + hover + 4 档宽度）
   - 3 lint 规则（unusedVariable + duplicateDefinition + missingColorAttribute）+ chip 三类细分
   - outline sheet 4 增强（搜索 + 排序 + 仅输出 + 双击跳转 + markdown 复制 + 依赖图）
   - tab 增强（拖动重排序 + ⌘\ split 视图）
   - 杂项（行末空格清理 + 编译错误一键复制）

2. **WP-54 模拟训练 trader 体验闭环**（25 batch · ~99.5%）
   - TrainingScenarioPattern 9 形态 + emoji + thumbnail 数据生成器（11 测试）
   - 三处 thumbnail 闭环（startSheet preset / history row / score sheet header）
   - history filter（pattern / period）+ sort（5 档）+ chip 分布 + streak
   - 月报 markdown（4 段 · 13 测试）+ 单次 markdown + PNG 截图分享
   - 周目标 + 本周/上周对比 + 今日 tally + 推荐时长进度条
   - score sheet（4 button + 4 快捷键 + 反馈 + 渐变 + 弹簧动画）+ 再练同形态/随机练

3. **WP-44 multichart 防误操作 + 指标扩充**（6 batch · ~99.5%）
   - CCI 副图（第 7 副图 · 中国期货顺势）+ W%R 副图（第 8 副图 · 短线反转）
   - Pivot Points 主图（5 线 R2/R1/PP/S1/S2 · 第 8 主图叠加）
   - cell 锁定（防误操作 swap/copy/reset）+ 批量锁定 + toolbar summary

4. **跨平台静态分析能力**（MaiLangLint + MaiLangDependencies）
   - 3 lint 规则 · 大小写不敏感 · tokenize 排除 string/comment
   - 变量依赖图（uses + usedBy）· 复用 outline.parse + tokenize
   - 18 个跨平台测试

5. **训练月度复盘工具链**（TrainingMarkdownReport）
   - 月报：概览 / 等级分布 / 形态分布 / 最近训练（4 段 markdown）
   - 单次：评分 / 形态 / 违规表
   - filter 应用：filterPattern + filterCutoff + filterLabel

6. **Linux 测试 ~1486/295 全绿**（+31 跨平台测试 · 6 新 suite · 0 回归）

7. **Mac 验收 batch105-163 = 59 commit 全部待批验**（累积 v15.18-v15.23 = 357 commit · 首次超大批量）

8. **已全部 push origin/main · 远程同步**

---

## 13 · 启动新会话推荐

直接复制这段开头新会话：

> 项目：中国期货 Mac/iPad 原生专业工作台 · v15.23 batch163 末状态衔接
>
> 【必读交接文件】
> /home/beelink/macos_tmp/macos_futures_trading_v1/会话交接-2026-05-06-v15.23-batch163末.md
>
> 【一句话状态】
> v15.23 batch105-163 共 59 commit 全 push · 三主线深度 polish 完成（WP-65 编辑器 IDE 级 100% / WP-54 训练 trader 闭环 / WP-44 multichart 防误操作 + CCI/W%R/Pivot）· Linux ~1486/295 全绿（+31 测试）· Mac 累积 357 commit 待集中验
>
> 【4 大新窗口入口】
> ⌘⇧T 模拟训练（★ 本 session 25 batch · trader 闭环）· ⌘⌥M 多图表（★ + CCI/W%R/Pivot/锁定）· ⌘⌥F 公式编辑器（★ 本 session 19 batch · IDE 级 100% 收尾）· ⌘R 复盘 · 全部统一 ⌘⇧? 帮助
>
> 【WP-65 公式编辑器 batch105-163 关键交付】
> - minimap：完整链 + 3 lint 规则 + outline 增强（搜索/排序/依赖/markdown）+ tab 拖动 + split 视图
> - 19 batch · 5 文件 2876 行 · ~99.99% IDE 级
>
> 【WP-54 模拟训练 batch105-163 关键交付】
> - thumbnail 三处闭环 + filter/sort + 月报/单次 markdown + PNG 分享 + streak + 周目标
> - 25 batch · 13 文件 3207 行 · ~99.5% trader 闭环
>
> 【WP-44 multichart batch105-163 关键交付】
> - CCI + W%R 副图（第 7/8）· Pivot Points 主图 · cell 锁定 + 批量
> - 6 batch · 4 文件 3533 行 · ~99.5%
>
> 请先读交接文件 · 然后回报"已了解" + 等下一步指令（🥇 Mac 切机集中验收 357 commit · 🥈 新 WP 启动（M6 节点）· 🥉 边缘 polish 价值已 LOW 不建议）。
