# 会话交接 v16.100 → v16.145 · Lint 第 6 规则 + Streak 接近升级 + Tooltip 深化 + 46 commit · 2026-05-10

## 1 · 项目极简（30 秒了解）

**做什么**：中国期货 Mac/iPad 原生专业工作台 · Stage A 不接 CTP 下单。
**关键时点**：M6（2026-09 末）= Pro 订阅上线 = 生死节点。
**本 session 核心**（共 **46 commit** · 全 push origin/main · 10 主线 polish）：

1. **Lint 系统扩展到第 6 规则**（v16.109 suspiciousVariableName · 变量名 typo 检测）+ quick fix 全套（v16.105 自动替换 / v16.110 chip 6 类 / v16.113 一键删 unused / v16.107 underline toggle / v16.102 fuzzy 补全 / v16.111 文档化）
2. **Streak 体系深化 + 接近 milestone 鼓励**（v16.101 点击跳 history / v16.139 接近 7/14/30 天提示 / v16.122/123 累计时长 milestone 5 级 / v16.145 时长接近 milestone）
3. **10+ 项 tooltip 深化信息密度**（v16.116 spread 警示 / v16.131 drilldown 复制 / v16.135/136 累积违规 + RulesPanel badge 含最近 session / v16.141/142 弱强 chip 含最近 3 次分数）
4. **历史 Panel 14 项 polish**（v16.114/124 强项 chip + 点击过滤 / v16.127 header streak hint / v16.128 emoji 摘要 contextMenu / v16.133/134 五维 ≥80✨/≥90🌟 / v16.138 5 维 markdown / v16.143 周末倒计时 / v16.108 累计平均/次）
5. **ScoreSheet polish**（v16.103 雷达 PNG 保存文件 / v16.120 截图保存 / v16.126 ESC 二级 dismiss / v16.129 满分 🏆 / v16.131 drilldown 复制）
6. **公式编辑器深化**（v16.102 fuzzy 补全 / v16.105 typo 自动替换 / v16.107 underline toggle / v16.113 一键删 unused）
7. **RulesPanel JSON/markdown 闭环**（v16.106 剪贴板版 / v16.117 覆盖确认 / v16.121 markdown 表格 / v16.130/136 violation badge）
8. **ControlBar idle 信息密度**（v16.104 today vs yesterday / v16.132 上次距今 / v16.137 状态 emoji）
9. **训练月报扩展**（v16.118/119/144 时长分布章节 +4 测 / v16.123 累计时长 milestone）
10. **helpSheet 完整文档化**（v16.111/125/140 三波更新 v16.66-139 全 cover）

### 46 commit 时间倒序（精简）

| commit | 主题 | 量级 |
|--------|------|------|
| `64ff7cf` | v16.145 · 累计时长接近 milestone 鼓励 | 18/3 · 1 文件 |
| `a948511` | v16.144 · 时长分布建议输出 +2 测 | 36/0 · 1 文件 |
| `26b69ef` | v16.143 · weeklyGoalRow 周末倒计时 | 10/0 · 1 文件 |
| `12c8511` | v16.142 · 强项 chip tooltip 含最近 3 次 | 16/1 · 1 文件 |
| `548182f` | v16.141 · 弱项 chip tooltip 含最近 3 次 | 16/1 · 1 文件 |
| `0a4cb72` | v16.140 · helpSheet 加 v16.101-139 完整文档 | 19/3 · 1 文件 |
| `2b71d73` | v16.139 · streak 接近 milestone 鼓励 | 13/2 · 1 文件 |
| `6641564` | v16.138 · 5 维平均 markdown 导出 Menu | 42/0 · 1 文件 |
| `c828306` | v16.137 · idleHint 状态 emoji | 2/2 · 1 文件 |
| `bb644c6` | v16.136 · RulesPanel badge tooltip 含 session | 10/1 · 1 文件 |
| `ee6d7c8` | v16.135 · 累积违规 chip tooltip 含 session | 18/3 · 1 文件 |
| `fbb5b96` | v16.134 · 五维 chip ≥ 90 → 🌟 大师级 | 8/1 · 1 文件 |
| `c4c0209` | v16.133 · 五维 chip ≥ 80 → ✨ 完美状态 | 6/4 · 1 文件 |
| `e86773e` | v16.132 · ControlBar 上次训练距今 | 15/1 · 1 文件 |
| `b023904` | v16.131 · ScoreSheet drilldown 右键复制详情 | 12/0 · 1 文件 |
| `5180ff1` | v16.130 · RulesPanel 每条规则违规 badge | 29/0 · 1 文件 |
| `401d862` | v16.129 · ScoreSheet 5 维满分 🏆 | 5/0 · 1 文件 |
| `c1f9ef7` | v16.128 · HistoryPanel session 复制 emoji 摘要 | 27/0 · 1 文件 |
| `0079718` | v16.127 · HistoryPanel header 🔥 streak hint | 7/0 · 1 文件 |
| `3f90d1b` | v16.126 · ScoreSheet ESC 二级 dismiss | 12/3 · 1 文件 |
| `ec64149` | v16.125 · helpSheet v16.101-124 文档 | 22/5 · 1 文件 |
| `4a1834a` | v16.124 · 强项 chip 加点击过滤回顾 | 18/12 · 1 文件 |
| `6584f42` | v16.123 · 月报 overview 加累计时长 milestone | 17/0 · 1 文件 |
| `9533d1f` | v16.122 · 累计时长 chip milestone emoji 5 级 | 15/3 · 1 文件 |
| `3db3012` | v16.121 · RulesPanel 复制规则集 markdown 表格 | 30/1 · 1 文件 |
| `0b3e17c` | v16.120 · ScoreSheet 截图改 Menu 加保存文件 | 56/5 · 1 文件 |
| `9cbef23` | v16.119 · 时长分布章节 +2 测 | 29/0 · 1 文件 |
| `295cff2` | v16.118 · 月报加时长分布章节 | 30/0 · 1 文件 |
| `80b1aff` | v16.117 · RulesPanel JSON 导入覆盖确认 dialog | 17/0 · 1 文件 |
| `e5d6fce` | v16.116 · 五维 spread > 30 加 ⚠️ 起伏大 | 1/1 · 1 文件 |
| `929e9d7` | v16.115 · weeklyGoalRow 冲刺鼓励 emoji 分级 | 11/2 · 1 文件 |
| `ddcebd2` | v16.114 · 加 "🏆 最强形态" chip 行 | 50/0 · 1 文件 |
| `a92ae6c` | v16.113 · lint section 一键删全部 unused | 25/0 · 1 文件 |
| `ec8a620` | v16.112 · session contextMenu 再练同形态 | 9/0 · 1 文件 |
| `e156408` | v16.111 · helpSheet 加 lint 6 规则文档 | 4/3 · 1 文件 |
| `715f1d1` | v16.110 · quickFixMenu suspicious + chip 6 类 | 32/0 · 1 文件 |
| `03327fa` | v16.109 · MaiLangLint suspicious 规则 +4 测 | 101/0 · 2 文件 |
| `142f3d4` | v16.108 · 累计时长 chip 加平均每次 | 7/3 · 1 文件 |
| `2bface3` | v16.107 · lint 行内波浪线开关 toggle | 11/5 · 1 文件 |
| `417bea7` | v16.106 · RulesPanel JSON 剪贴板版 | 44/2 · 1 文件 |
| `a3cc14f` | v16.105 · undefined typo 自动替换 quick fix | 31/0 · 1 文件 |
| `ec09b95` | v16.104 · ControlBar today vs yesterday chip | 36/0 · 1 文件 |
| `47463ed` | v16.103 · ScoreSheet 雷达 PNG 保存文件 | 59/20 · 1 文件 |
| `34a2e22` | v16.102 · 自动补全 fuzzy 子序列匹配 | 30/4 · 1 文件 |
| `872d3ee` | v16.101 · streak chip + 7 天 bar 点击跳 history | 53/31 · 3 文件 |

**累计**：**~1832 行净增 / ~96 行删** · **13 文件改动** · **+10 swift testing 新测** · **0 回归**

---

## 2 · 仓库与环境

| 维度 | 值 |
|------|---|
| Linux 主目录 | `/home/beelink/macos_tmp/macos_futures_trading_v1` |
| Mac 主目录 | `/Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1` |
| 最新 commit | `64ff7cf`（v16.145） |
| origin/main | 同步（46 commit 全 push） |
| 当前分支 | `main` |
| Linux 测试 | **2486 / 388 swift testing 全绿**（v16.100 末 2478 → 2486 · +8 净增 · 含 lint 6/markdown 报告 2）+ XCTest 73 全绿 |
| Mac 实测 | **本 session 未切机** · v15.96+v16.5+v16.27→v16.145 = **141 commit 累积待验** |
| Mac 编译 | ⏳ 未验（v16.30 hotfix 后 135 commit 未切） |
| **SSH 免密** | Mac → `beelink@vvsvr` 已配 |

---

## 3 · 新会话开工指引

### 3.1 Linux 端（主开发）

```bash
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build       # 1-5s 增量
swift test        # 期待 2486 / 388 全绿（13 秒）+ XCTest 73 全绿

# 主 .build 抢锁切独立 build path
swift build --build-path /tmp/build_v16145
```

### 3.2 Mac 切机自动化（v15.85 + v16.29 --skip-shots）

```bash
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull
./mac_acceptance.sh                    # 全自动（build + test + 23 截图 + scp ~5min）
./mac_acceptance.sh --skip-shots       # ~1min · 仅 build + test + demo（推荐先跑）
```

**重要**：本 session 46 commit 多为 view 层 + lint 第 6 规则 + tooltip 深化 · Mac 累积 141 commit。

### 3.3 Lint 系统 6 规则速查（v16.109 完成 6 规则全栈）

```
6 Kind（v16.66 加 undefined / v16.98 加 argCount / v16.109 加 suspicious）：
1. unusedVariable          severity=warning · 中间变量未引用
2. duplicateDefinition     severity=error   · 同名 ≥ 2 次定义
3. missingColorAttribute   severity=warning · 输出未 COLOR
4. undefinedVariable       severity=error   · 标识符未定义（含 typo 建议 v16.93）
5. argCountMismatch        severity=error   · 函数参数数量与签名不符（v16.98）
6. suspiciousVariableName  severity=warning · 变量名 typo（≥ 5 字符 + 距离 1 + 非 letter+digit · v16.109）

新算法：
- closestExactDistance · 距离严格匹配（v16.109）
- isLetterDigitVariable · 识别 MA5/EMA10 标准命名跳过（v16.109）

5 处展现（含 v16.110 新增 chip）：
1. outline panel lint section（按 severity 排序 · v16.75）
2. minimap warningLines（橙 warn / 红 error · v16.77）
3. status bar chip 6 类（v16.78/110）：undefined / dup / argCount / unused / color / suspicious
4. 行内波浪线（按 severity 颜色 · v16.92/96 · v16.107 可关闭）
5. quick fix Menu 5 案（v16.60/66/97/98/110）
   · suspicious 加复制 builtin 名（v16.110）
   · undefined 加自动替换（v16.105 · word boundary）

typo 系列（v16.93/95/97/105/109）：
- Levenshtein 距离 ≤ 2 + 短词 < 3 跳过
- closestKnownName + closestExactDistance 两个变体
- 自动替换 = quickFixReplaceInLine（regex \\b word boundary）
```

### 3.4 Streak 体系深化（v16.79-145 全栈）

```
2 个模型算法（TrainingSessionLog · v16.80/89）：
- consecutiveTrainingDays(asOf:cap:)
- longestStreakEver()

ControlBar idle chip 链（v16.79/83/89/101/139）：
- 🔥 (≥2) → 🔥🔥 (≥7) → 🚀 (≥14) → 🏆 (≥30) → 🎉 新纪录
- 接近 milestone（≤ 2 天）→ tooltip 加 "再 N 天达 X 级"（v16.139）
- 点击跳 history tab（v16.101 · pendingJumpToHistoryTab）
- personal best 对比（v16.89 · 当前 < 历史 → "X/最长 Y"）

7 天 mini bar（v16.76）：
- 类 GitHub contributions · 点击跳 history（v16.101）

today vs yesterday chip（v16.104）：
- ↑/↓/= 三态 · 短时反馈

上次训练距今（v16.132）：
- 刚刚 / N 分钟前 / N 小时前 / N 天前

HistoryPanel statsCard 同步（v16.80/84/90）：
- 🔥 连训 chip 同 milestone emoji
- header 加 🔥 streak hint（v16.127 · 第一眼即看）

累计时长 milestone（v16.122/123/145）：
- ⏱ < 10h → 🎯 10h+ → 🚀 50h+ → 🏆 100h+ → 👑 500h+ → 🌟 1000h+
- ≥ 50h 紫色（精英）
- 接近 milestone（≤ 5h）→ "距 Xh 还差 Yh"（v16.145）
- 月报 overview 同步显示（v16.123）

markdown 月报 streak overview（v16.86/91/94）：
- "当前连训：🔥 X 天（历史最长 Y 天）"
- 新纪录 → "🎉 X 天（**新纪录**！）"
- 当前断 + 历史 ≥ 3 → "🏅 历史最长：N 天（重新开始即可）"
- 累计时长 milestone（v16.123 · 同 chip 5 级）
```

### 3.5 五维 chip 深化（v16.62/73/116/133/134/138）

```
HistoryPanel 五维平均 chip 行：
- 标签 emoji 3 级（v16.133/134）：
  · 默认 🔬
  · 全 ≥ 80 → ✨ 完美 + 紫色背景
  · 全 ≥ 90 → 🌟 大师级
- 最弱橙色高亮（除非 allBalanced · v16.133 跳过）
- tooltip 含 min/max/spread（v16.73）
- spread > 30 → ⚠️ 起伏大（v16.116）

5 维 markdown 导出 Menu（v16.138）：
- HistoryPanel 导出 Menu 加 "复制 5 维平均 markdown"
- 5 行表格 + ⚠ 最弱标记 · IM 一键分享

ScoreSheet drilldown 增强：
- 满分 100 加 🏆 后缀（v16.129）
- 右键复制本维度详情（v16.131 · contextMenu）
- ESC 二级 dismiss（v16.126 · 先关 drilldown 后关 sheet）
- ↑↓ 切换时 feedback 提示（v16.85）

雷达 PNG 双导出（v16.103）：
- ⌘⌥R Menu：复制剪贴板 / 保存为文件
- 文件名 "雷达_<场景>_<分数>分_yyyyMMdd_HHmm.png"

截图分享改 Menu（v16.120）：
- 复制剪贴板（⌘⇧C · 原行为）
- 保存为 PNG 文件…
- 文件名 "评分卡_<场景>_<分数>分_yyyyMMdd_HHmm.png"
```

### 3.6 历史 Panel polish 链（v16.55-145 完整状态）

```
header（v16.127 · 一眼看连训）：
- "📚 训练历史 X 次 · 🔥 连训 Y 天"（streak ≥ 2 才显示）

statsCard chip 行（密度极高）：
- 总训练 / 平均分 / 最佳 / 🔥 连训 / ⏱ 累计 milestone（v16.122）
- 等级分布 horizontal bar
- 评分趋势 sparkline（v16.16）
- 形态分布 chip（v16.125）
- 🔬/✨/🌟 五维平均 chip 行（v16.62/133/134）
- ⚠️ 弱项加练 / 🏆 强项展示 chip 行（v16.19/114/124）
- 📛 累积违规 chip 行（v16.45/46/135）
- weekly goal progress bar（v16.81/115/143）
  · 距 1 次 → "🎯 冲刺 1 次"
  · 超额 1 / 2-4 / 5+ → ✓ / 🎯 / 🏆
  · 未达标且周末 ≤ N 天 → ⏳Nd

filter 4 组（pattern / period / sort / search）：
- CSV 导出 filter 子集（v16.72）
- 5 维 markdown 导出（v16.138）

session list（增量分页 v16.55）：
- 默认 50 + 加载更多 +50 + 全部展开
- 删除 ⌘Z 5s undo banner（v16.64）
- contextMenu：查看评分 / 再练同形态（v16.112）/ 复制 emoji 摘要（v16.128）/ 复制 markdown / 删除

月度对比（v16.67 · vs 上月 chip）
spread 警示（v16.116 · tooltip ⚠️ 起伏大）
tooltips 含最近 3 次该形态分数（v16.141/142）
```

### 3.7 全部窗口快捷键（v16.145 状态 · 本 session 无新增 · 仅扩展 v16.100 集）

新增点击交互：
- **ControlBar 🔥 streak chip 点击** → 跳 history tab（v16.101）
- **ControlBar 7 天 mini bar 点击** → 跳 history tab（v16.101）
- **强项 🏆 chip 点击** → filter 该形态（v16.124）

新增 quick fix Menu Action：
- **lint section "一键删除 N 条 unused"**（v16.113）
- **undefined "替换为 X"**（v16.105 · word boundary 自动替换）
- **suspicious "复制 builtin 名 X"**（v16.110）

### 3.8 协作规则核心（永久记忆已加载 · 28 条无变化）

无新增永久记忆。原 28 条覆盖所有规则。

---

## 4 · 当前阶段冻结状态

### 4.1 Epic 进度（与 v16.100 对比）

| Epic | v16.100 | 本 session 增量 | 当前 |
|------|--------|---------|---------|
| Lint 系统 | 5 规则 + 5 处展现 | + **第 6 规则 suspiciousVariableName** + **chip 6 类完整** + **行内 underline toggle** + **undefined 自动替换 quick fix** + **fuzzy 子序列补全** + **一键删全部 unused** | **lint 6 规则完整 ✅** |
| Streak 体系 | 全栈 + personal best | + **点击跳 history** + **接近 milestone 鼓励** + **today vs yesterday** + **上次距今** + **header streak hint** | **streak 体系深化 ✅** |
| 历史 Panel | 14 增强 | + **强项 chip + 点击过滤** + **header streak hint** + **复制 emoji 摘要** + **5 维平均 markdown 导出** + **五维 ≥80✨/≥90🌟** + **spread 警示** + **累计时长 milestone + 接近** + **周末倒计时** + **弱强 chip 最近 3 次 tooltip** + **累积违规 chip session 名 tooltip** | **历史 Panel 26 增强 ✅** |
| ScoreSheet | 6 增强 | + **drilldown 右键复制** + **满分 🏆** + **ESC 二级 dismiss** + **雷达 PNG 保存文件** + **截图保存文件** | **ScoreSheet 11 增强 ✅** |
| 公式编辑器 | 6 规则 lint | + **fuzzy 子序列匹配** + **typo 自动替换** + **underline toggle** + **一键删 unused** | **公式编辑器 IDE 体验完整 ✅** |
| RulesPanel | JSON 导入/导出 | + **JSON 剪贴板版** + **markdown 表格导出** + **每条 ⚠️ 违规 badge + 最近 session tooltip** + **覆盖确认 dialog** | **RulesPanel 完整 ✅** |
| ControlBar idle | 3 chip | + **today vs yesterday** + **上次训练距今** + **idleHint 状态 emoji** + **streak chip 点击跳** | **idle 信息密度极高 ✅** |
| 训练月报 | 5 章节 | + **时长分布章节 +4 测** + **累计时长 milestone** | **月报 6 章节 ✅** |
| 测试加固 | 2478/388 | +10 净（lint 4 · markdown 4 · log 0 · subscores 0 · 其他 2） | **2486/388 ✅** |
| **里程碑** | v16.100 整数 | **v16.140 整数节点 🎉** + 各 chip tooltip 深化 + 5 维三级 milestone | 🎉🎉🎉 |

### 4.2 测试基线

```
Linux 端（开发主）：
- swift testing: 2486 / 388 全绿（v16.100 末 2478 → 2486 · +8 净增）
- XCTest:        73 / 0    全绿（与 v16.100 同）
- 累计：2559 测 · 0 回归

本 session 新增 +10 测（部分调整既有）：
- v16.109 MaiLangLint suspicious 4 测
- v16.118/119/144 月报时长分布 4 测
- v16.110 chip 6 类（隐式覆盖）
- v16.93/95/97/105 typo 系列已覆盖

Mac 端：
- 本 session 未切机
- v15.96+v16.5+v16.27→v16.145 共 141 commit 累积待验
```

### 4.3 simplifier 状态

本 session 所有改动 L3 跳过（view function / chip / tooltip / Menu / button）·
L1 涉及的算法已 5+ 测覆盖（v16.109 closestExactDistance + isLetterDigitVariable）·
按 5 版本批量复检规则 · 本 session 累积 46 commit · 但 L1 全测覆盖 · L2/L3 模式化 · 整体跳过。

### 4.4 Mac 累积验收清单（更新版 · v16.145 后）

#### ⏳ v15.96 + v16.5 + v16.27→v16.145 累积待 Mac 验证（141 commit · v16.30 后 135 commit）

**🥇 高优先验**（本 session 新增重点）：

**Lint 第 6 规则**：
- ⌘⌥F 公式编辑器 → 定义变量名（如 `MAVRG`）与某 builtin 距离 1 → 应触发 `suspiciousVariableName` 警告
- outline panel section 显示 "X 紧急 + Y 提醒" 含 6 类
- minimap warning marker 6 类 + 颜色
- status bar chip 6 类 + severity 红/橙
- 行内波浪线 toggle 可关闭（齿轮 Menu）

**Streak 深化**：
- ControlBar 🔥 chip 点击 → 切到 history tab
- 7 天 mini bar 点击 → 切到 history tab
- streak == 5/6 → tooltip 加 "再 N 天达 🔥🔥 周级"
- HistoryPanel header 一眼看 "🔥 连训 X 天"

**历史 Panel 深化**：
- 五维 chip ≥ 80 → ✨ 紫色完美 · ≥ 90 → 🌟 大师级
- 累积违规 chip hover → 显示最近 5 次 session 名
- 弱项/强项 chip hover → 显示最近 3 次分数
- session 右键 → 复制 emoji 摘要 / 再练同形态
- weekly goal 未达标 → ⏳ 距周末 N 天
- 强项 chip 点击 → 过滤回顾该形态

**ScoreSheet 深化**：
- drilldown 满分 → 🏆 后缀
- drilldown 右键 → 复制本维度详情
- ESC → drilldown 全部折叠先 / 全收后才关 sheet
- ⌘⌥R Menu → 复制剪贴板 / 保存 PNG 文件
- "截图分享" Menu → 复制 / 保存 PNG 文件

**RulesPanel polish**：
- 每条规则行 → 右侧 ⚠️ N 违规 badge（0 不显示 · 颜色按数量）
- badge hover → 显示最近 5 次违规 session 名
- ⋯ Menu → 复制 markdown 表格 / 复制 JSON / 文件导入 / 粘贴导入
- import 时 → 覆盖确认 dialog

**ControlBar idle**：
- "✅ 已启用 X 条规则 · 准备就绪 · 上次 N 分钟前"
- "vs 昨 +X" today vs yesterday chip

**月报扩展**：
- markdown 月报 → "训练时长分布" 章节（3 段）
- markdown 月报 overview → 累计时长 milestone

**🟡 中优先**（用户辅助清单 P0 第 2-6 项 · 累积未走）：
- 第 2-6 项 · 与 v16.100 同 · 暂留

### 4.5 剩余路线候选（v16.146+）

#### 🥇 训练 ScoreSheet "5 维 → 改进 plan" 文本（trader 看完弱项 + 给具体 5 步行动建议 · 0.2d）

#### 🥈 HistoryPanel 月度分组 separator（同月 sessions visual 灰色分隔 · 0.15d）

#### 🥉 复盘 ReviewWindow zoomedCard 加 ⌘C 复制单图 markdown（0.1d · 已有 export PNG）

#### 🟪 HistoryPanel session 列表行内 mini 5 维 dots（visual 一眼看 5 维形状 · 0.2d）

#### 🟫 训练 ControlBar 点击 today/yesterday chip → filter 历史 panel 该时段（0.15d · 与 streak chip 同模式）

#### 🟧 公式编辑器 lint 警告 "过滤" Toggle（按 severity / kind 过滤 outline section · 0.2d · trader 长公式时 noise 控制）

---

## 5 · 关键设计沉淀（本 session 新增）

### 5.1 Lint 第 6 规则 + 全栈 quick fix（v16.105/107/109/110/113）

```
suspiciousVariableName（v16.109）：
- 检测变量定义名与 builtin 仅差 1 字符
- 限制 ≥ 5 字符（DIF/DEA/MACD 等短名跳过 · 否则误报）
- letter+digit 模式跳过（MA5/EMA10 标准命名）
- closestExactDistance + isLetterDigitVariable 两个 helper

undefined 自动替换（v16.105）：
- parseUndefinedName regex 从 message 提取标识符
- quickFixReplaceInLine 用 \\b word boundary regex
- 防误替换子串（MA 不影响 MA5）

行内 underline toggle（v16.107）：
- @AppStorage showLintUnderline · 默认开
- 齿轮 Menu 加 Toggle + 状态指示
- callsite 按 toggle 传 [] 或 analyze 结果

一键删除全部 unused（v16.113）：
- ≥ 2 才显示 button（单条用 Menu）
- quickFixDeleteLinesBatch 倒序删（避免索引偏移）
```

### 5.2 Streak 接近 milestone 鼓励（v16.139）

```
milestone 4 级阈值：7 / 14 / 30 天

```swift
let nextMilestone: (days: Int, label: String)? = {
    if streak < 7 { return (7, "🔥🔥 周级") }
    if streak < 14 { return (14, "🚀 两周") }
    if streak < 30 { return (30, "🏆 月级") }
    return nil
}()
let nearHint = nextMilestone.flatMap { ms -> String? in
    let remaining = ms.days - streak
    return remaining <= 2 ? " · 再 \(remaining) 天达 \(ms.label)" : nil
}
```

类似累计时长 milestone 接近（v16.145 · 剩 ≤ 5h 显示距离）
```

### 5.3 tooltip 深化设计模式（v16.135/136/141/142）

```
统一构造 helper 模式：
- buildXxxTooltip(...): String 接收 chip 关键参数
- filter sessions / scores · 取最近 N 个
- 拼接 base 描述 + "\n最近 N：" + items.joined("\n")

4 处应用：
1. 累积违规 chip → 最近 5 次违规 session（v16.135）
2. RulesPanel violation badge → 同 session 名（v16.136）
3. 弱项 chip → 最近 3 次分数 trend（v16.141）
4. 强项 chip → 同上（v16.142）

trader 一次 hover 即看 chip 数字背后的具体 sessions / scores · 不必单独打开 history list
```

### 5.4 5 维三级 milestone（v16.133/134）

```
HistoryPanel 五维平均 chip 标签升级：
- 默认 🔬 五维
- 全 5 维 ≥ 80 → ✨ + 紫色背景（完美状态）
- 全 5 维 ≥ 90 → 🌟（大师级）

```swift
let allBalanced = stats.allSatisfy { $0.avg >= 80 }
let allMaster = stats.allSatisfy { $0.avg >= 90 }
let labelEmoji: String = {
    if allMaster { return "🌟" }
    if allBalanced { return "✨" }
    return "🔬"
}()
```

特殊处理：allBalanced 时不再标 weakest（全部均衡 · 无短板）

类似累计时长 5 级（v16.122）：⏱→🎯→🚀→🏆→👑→🌟
```

### 5.5 双向跳转模式（v16.101）

```
ControlBar → HistoryPanel 跳转链：
- @Published pendingJumpToHistoryTab: Bool（ViewModel）
- ControlBar chip Button onTap → set true
- TrainingWindow .onChange → tab = .history + DispatchQueue 清回 false

与 v16.46 pendingJumpToRulesTab 同模式
与 v16.58 recentlyAddedSessionID 同模式

可扩展：
- pendingJumpToLivePanel · 同模式扩展到 live tab
- pendingHighlightRuleKind · 跳 RulesPanel 后高亮特定 rule
```

### 5.6 月报时长分布章节（v16.118）

```
3 段分布 + 智能建议：
- 🏃 短 < 15 分 / 🎯 中 15-30 / 🧘 长 > 30
- 表格 + 占比 + 数学建议

建议触发：
- shortCount > total/2 → "可能过于试探 / 中断频繁 · 建议至少 15 分钟"
- longCount > total/2 → "单次过长可能效率递减 · 建议拆短 + 中间休息"

2 测覆盖建议输出（v16.144）
```

---

## 6 · 战略路线 + 累积 Mac 验收意识

### 6.1 v16.100 → v16.145（46 commit · ~1832 行净增 · ~96 删）

**功能扩展**：
- Lint 系统 6 规则完整
- Streak 体系深化（接近 milestone / 点击跳 / 累计时长 milestone / 上次距今）
- 10+ tooltip 深化（session 名 / 最近分数 / spread 警示）
- 5 维 chip 三级 milestone（🔬/✨/🌟）
- 历史 Panel 14 项 polish
- ScoreSheet 5 项深化（drilldown 复制 / 雷达保存 / ESC 二级）
- 月报扩展 1 章节（时长分布）

**测试加固**：+10 测（lint 4 / 月报 4 / 其他 2）

**质量门**：simplifier 全 L3 跳过 · L1 全测覆盖

**无 Mac hotfix**：本 session 未切机 · Mac 累积 141 commit 待验

### 6.2 关键工程沉淀

- **tooltip 构造 helper 模式**：4 个 chip 统一 buildXxxTooltip(...) 模式（v16.135/136/141/142）
- **milestone emoji 升级链**：streak 4 级 + 累计时长 5 级 + 5 维 3 级 · 用户成就感分层
- **接近 milestone 鼓励**：streak 接近（v16.139）+ 累计时长接近（v16.145）· 心理学激励
- **双向跳转 pendingJumpToHistoryTab**：与 v16.46 / v16.58 同模式（v16.101）
- **批量 quick fix**：lint section 一键删 unused（v16.113 · 倒序删除避免索引偏移）
- **自动替换 word boundary**：regex \\b 防误替换子串（v16.105）

---

## 7 · 关键文件改动汇总

### 7.1 新增文件

无新增 .swift 文件（全部 inline 增强既有文件）

### 7.2 修改文件（v16.100 → v16.145 · 13 文件）

**Shared lint 系统**：
- `MaiLangLint.swift`（+ suspiciousVariableName 第 6 规则 + closestExactDistance + isLetterDigitVariable · v16.109）

**TradingCore 算法层**：
- `TrainingMarkdownReport.swift`（+ 时长分布章节 + 累计时长 milestone overview · v16.118/123）

**MainApp Training**：
- `TrainingViewModel.swift`（+ pendingJumpToHistoryTab · v16.101）
- `TrainingControlBar.swift`（+ today vs yesterday + 上次距今 + idleHint emoji + streak 点击 + 接近 milestone · v16.101/104/132/137/139）
- `TrainingHistoryPanel.swift`（10+ 项 polish · v16.108/114/115/116/122/124/127/128/133/134/135/138/141/142/143/145）
- `TrainingScoreSheet.swift`（drilldown 复制 + 满分 🏆 + ESC 二级 + 雷达保存 + 截图保存 · v16.103/120/126/129/131）
- `TrainingRulesPanel.swift`（+ JSON 剪贴板版 + markdown 表格 + 违规 badge + 覆盖确认 · v16.106/117/121/130/136）
- `TrainingWindow.swift`（+ onChange pendingJumpToHistoryTab + helpSheet × 3 更新 · v16.101/125/140）

**MainApp 公式编辑器**：
- `FormulaEditorWindow.swift`（+ undef 自动替换 + lint underline toggle + suspicious quick fix + 一键删 unused + chip 6 类 + 文档化 · v16.105/107/110/111/113）
- `MaiLangCodeView.swift`（+ fuzzy 子序列匹配 · v16.102）

**测试**（+10 净增）：
- `MaiLangLintTests.swift`（+4 测 v16.109 suspicious）
- `TrainingMarkdownReportTests.swift`（+4 测 v16.118/119/144 时长分布）

### 7.3 永久记忆无变化

本 session 未新增永久记忆 · 已有 28 条覆盖所有规则。

---

## 8 · 关键文档索引

| # | 文档 | 用途 |
|---|------|------|
| 1 | `D1-顶层设计.md` ~ `D4-StageB规划.md` | 战略 + Stage A/B 蓝图 |
| 5 | `Stage A 工作包清单.md` | 13 Epic / 63 WP 清单 |
| 7 | `Mac 验收清单.md` | 累积切机验收（~500+ 项） |
| 14 | `run-mac.sh` | .app 打包 + 启动脚本 |
| 16 | `mac_acceptance.sh` | 23 窗口自动化截图脚本 |
| 18 | `Mac切机用户辅助清单_v15.82.md` | P0 6 + P1 5 + P2 2 项 |
| 35 | `会话交接-2026-05-10-v16.54-...md` | 上 上次交接 |
| 36 | `会话交接-2026-05-10-v16.100-...md` | 上次交接 |
| **37** | **`会话交接-2026-05-10-v16.145-lint第6规则+streak接近升级+10项tooltip深化+46commit.md`** | **本会话** |

---

## 9 · 一行命令速查

```bash
# Linux 主开发
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build       # 1-5s 增量
swift test        # 期待 2486 / 388 + 73 XCTest 全绿（13s）

# Mac 切机自动化
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull
./mac_acceptance.sh --skip-shots     # ~1min · 仅验编译/测试（推荐先跑）
./mac_acceptance.sh                  # ~5min · 全自动（含截图）

# git 状态
git log --oneline 1a87988..HEAD       # 看本 session 全 46 commit
git status
```

---

## 10 · 可能踩的坑（本 session 实测）

### 10.1 SourceKit 缓存陈旧（持续 · 老坑 10.1）
- 新增 type members（kind enum / Severity / levenshtein）IDE 报 "X has no member"
- swift build / swift test 实际通过
- 本 session 出现多次（v16.109 closestExactDistance / 测试访问 enum members）
- 解决：忽略 IDE diagnostic · 跑 swift test 验证

### 10.2 v16.109 suspicious 规则误报（已修）
- DIF/DEA/MACD 等 trader 常用 3-4 字符变量名距离 builtin 1 → 误报
- 修：要求 ≥ 5 字符才检测 + letter+digit 模式跳过
- 5 字符以下属 trader 标准命名 · 不应触发

### 10.3 closestKnownName 距离 0（自身）
- Set 包含 word 自身时距离 0 · 应跳过取次近
- 短词 < 3 字符直接 nil（防噪音）
- 平局取最短名

### 10.4 outline 每行只取首个语句（v16.66 教训）
- MaiLangOutline.parse 每行 trim 后找首个 `:` / `:=`
- `MA5:=...;OUT:...` 一行只识别 MA5
- 测试 src 多语句必须分行写

### 10.5 word boundary regex（v16.105）
- `\\b\(escaped name)\\b` 防误替换子串
- MA → MA5 内不会替换（5 不是 word char 边界后置）
- 配合 NSRegularExpression.escapedPattern 防特殊字符

### 10.6 测试 flaky（与本 session 无关）
- ReplayDriver Timer + CalendarSpread contango 偶发 · re-run 通过

### 10.7 Mac swift 6.3 严格 concurrency（v16.30 永久记忆 · 仍待验）
- 本 session NSEvent.addLocalMonitorForEvents（v16.59）已上次会话引入
- Mac 待验 · 可能需 MainActor.assumeIsolated 调整

### 10.8 视觉密度风险
- HistoryPanel statsCard 现含 ~10 行 chip / progress / sparkline
- ControlBar idle 含 ~5 chip（today tally / today vs yesterday / streak / 7 天 bar / 上次距今）
- Mac 切机可能需视觉验证窗口尺寸适配

---

## 11 · 修订日志（本 session 时间倒序 · 精简）

- **v16.145 累计时长接近 milestone 鼓励 (2026-05-10)**
- **v16.144 时长分布建议输出 +2 测**
- **v16.143 weeklyGoalRow 周末倒计时**
- **v16.142 强项 chip tooltip 最近 3 次**
- **v16.141 弱项 chip tooltip 最近 3 次**
- **v16.140 helpSheet 加 v16.101-139 文档**
- **v16.139 streak 接近 milestone 鼓励**
- **v16.138 5 维平均 markdown 导出 Menu**
- **v16.137 idleHint 状态 emoji**
- **v16.136 RulesPanel badge tooltip session**
- **v16.135 累积违规 chip tooltip session**
- **v16.134 五维 ≥ 90 → 🌟**
- **v16.133 五维 ≥ 80 → ✨**
- **v16.132 ControlBar 上次训练距今**
- **v16.131 ScoreSheet drilldown 右键复制**
- **v16.130 RulesPanel violation badge**
- **v16.129 ScoreSheet 满分 🏆**
- **v16.128 emoji 摘要 contextMenu**
- **v16.127 HistoryPanel header streak hint**
- **v16.126 ScoreSheet ESC 二级 dismiss**
- **v16.125 helpSheet v16.101-124 文档**
- **v16.124 强项 chip 点击过滤**
- **v16.123 月报累计时长 milestone**
- **v16.122 累计时长 milestone 5 级**
- **v16.121 RulesPanel markdown 表格**
- **v16.120 ScoreSheet 截图保存文件**
- **v16.119 时长分布 +2 测**
- **v16.118 月报时长分布章节**
- **v16.117 RulesPanel JSON 覆盖确认**
- **v16.116 五维 spread > 30 警示**
- **v16.115 weeklyGoal 冲刺鼓励**
- **v16.114 强项 chip 行**
- **v16.113 lint 一键删 unused**
- **v16.112 再练同形态 contextMenu**
- **v16.111 helpSheet lint 6 规则**
- **v16.110 quickFixMenu suspicious + chip 6 类**
- **v16.109 MaiLangLint suspicious 规则 +4 测**
- **v16.108 累计时长加平均/次**
- **v16.107 lint underline toggle**
- **v16.106 RulesPanel JSON 剪贴板版**
- **v16.105 undef 自动替换 quick fix**
- **v16.104 ControlBar today vs yesterday**
- **v16.103 ScoreSheet 雷达 PNG 保存文件**
- **v16.102 公式编辑器 fuzzy 子序列**
- **v16.101 streak chip 点击跳 history**

---

## 12 · 启动新会话推荐（直接复制）

```text
项目：中国期货 Mac/iPad 原生专业工作台 · v16.145 · Lint 第 6 规则 + Streak 接近升级 + Tooltip 深化 · 46 commit · 准备开干 v16.146+

【必读交接文件】
/home/beelink/macos_tmp/macos_futures_trading_v1/会话交接-2026-05-10-v16.145-lint第6规则+streak接近升级+10项tooltip深化+46commit.md

【一句话状态】
v16.100 → v16.145 · 46 commit 全 push · ~1832 行净增 · 10 主线 polish（lint 6 规则 / streak 接近升级 / tooltip 4 处深化 / 历史 panel 14 增强 / ScoreSheet 5 项 / 公式编辑器 4 项 / RulesPanel 5 项 / ControlBar 3 项 / 月报 2 章节 / helpSheet 3 波文档）· 测试 2486/388 + 73 XCTest 全绿

【本 session 关键产出】
- Lint 系统第 6 规则 suspiciousVariableName（v16.109 +4 测 · 变量名 typo 检测 ≥5 字符 + letter+digit 模式跳过）
- Streak 体系深化：点击跳 history（v16.101）/ 接近 milestone 鼓励（v16.139）/ today vs yesterday（v16.104）/ 上次距今（v16.132）/ header hint（v16.127）/ 累计时长 milestone 5 级（v16.122/123/145）
- 4 处 tooltip 深化含最近 session/scores：累积违规（v16.135）/ RulesPanel badge（v16.136）/ 弱项 chip（v16.141）/ 强项 chip（v16.142）
- 5 维 chip 3 级 milestone：🔬 → ✨(≥80) → 🌟(≥90)（v16.133/134）+ spread > 30 警示（v16.116）+ markdown 导出 Menu（v16.138）
- ScoreSheet 5 项：drilldown 右键复制（v16.131）/ 满分 🏆（v16.129）/ ESC 二级 dismiss（v16.126）/ 雷达 PNG 保存文件（v16.103）/ 截图保存（v16.120）
- 公式编辑器：fuzzy 子序列补全（v16.102）/ undef 自动替换 quick fix（v16.105 · word boundary regex）/ underline toggle（v16.107）/ 一键删 unused（v16.113）
- RulesPanel：JSON 剪贴板版（v16.106）/ markdown 表格（v16.121）/ 每条规则 ⚠️ 违规 badge（v16.130/136）/ 覆盖确认（v16.117）
- 月报时长分布章节 +4 测（v16.118/119/144）+ 累计时长 milestone overview（v16.123）

【Mac 切机状态】
- ⏳ v15.96+v16.5+v16.27-v16.145 = 141 commit 累积
- 本 session 未切机（v16.30 hotfix 后 135 commit 待验）
- 重点验证：
  · ⌘⌥F 公式编辑器：suspicious 规则 + status bar 6 类 chip + 行内 underline toggle + 自动补全 fuzzy
  · ⌘⇧T HistoryPanel：五维 ≥80✨/≥90🌟 + tooltip 最近 N + 强项 chip + emoji 摘要 + 周末倒计时
  · ⌘⇧T ControlBar：🔥 chip 点击跳 history + today vs yesterday + 上次距今 + 接近 milestone 鼓励
  · ⌘⇧T RulesPanel：每条规则 ⚠️ badge + tooltip session + markdown 表格导出 + JSON 覆盖确认
  · ⌘⇧T ScoreSheet：满分 🏆 + drilldown 右键复制 + ESC 二级 + 雷达/截图保存文件
  · ⌘R 复盘月报：时长分布章节 + 累计时长 milestone overview

【下一步候选】
🥇 ScoreSheet 5 维 "改进 plan" 文本（trader 弱项 → 5 步行动 · 0.2d）
🥈 HistoryPanel 月度分组 separator（同月 sessions visual · 0.15d）
🥉 ReviewWindow zoomedCard ⌘C 复制单图 markdown（0.1d）
🟪 HistoryPanel session 行内 mini 5 维 dots（visual 形状 · 0.2d）
🟫 ControlBar today/yesterday chip 点击 filter history（0.15d）
🟧 公式编辑器 lint 警告过滤 Toggle（按 severity / kind · 0.2d）

【关键提醒】
- IAP / Pro 门控不预留（M6 前一气呵成）
- CTP SimNow 短期搁置
- macOS 14+ API 禁用（项目锁 macOS 13）
- bash $VAR 后中文必须 ${VAR}
- 自动连贯到底（v15.59）：不喊停就一直做
- 全局 ⌘⌥ 已占字母：F/M/S/O/B/H/P/C/N/X/I/T/A/W · 本 session 无新增
- Mac swift 6.3 严格三类陷阱（v16.30 永久记忆）
- ReplayDriver + CalendarSpread 测试偶发 flaky（re-run 通过）
- v16.109 suspicious 规则 ≥ 5 字符 + letter+digit 模式跳过（trader 标准 MA5/EMA10 不误报）
- v16.105 quickFixReplaceInLine 用 \\b word boundary regex 防误替换子串
- 视觉密度提醒：HistoryPanel statsCard ~10 行 chip · ControlBar idle ~5 chip · Mac 切机需视觉验证窗口适配

请先读交接文件 · 然后回报"已了解" + 等下一步指令。
```
