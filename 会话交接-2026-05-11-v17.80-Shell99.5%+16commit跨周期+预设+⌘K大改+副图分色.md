# 会话交接 v17.64 → v17.80 · Shell 95% → ~99.5% · 16 commit 已 push · 2026-05-11

## 1 · 项目极简（30 秒了解）

**做什么**：中国期货 Mac/iPad 原生专业工作台 · Stage A 不接 CTP 下单
**关键时点**：M6（2026-09 末）= Pro 订阅上线 = 生死节点
**本 session 核心**（共 **16 commit · 16 语义化版本 · 全部已 push**）：

接 v17.64 差距清单"剩 3 项门槛 + 候选 4 polish"· 推进 16 项功能 · Shell 完整度 95% → **~99.5%**（v17.0 §13.2 设计 7 项完成 6/7）· TradingView 99% 不变 · 测试 2657 全绿 16/16 commit。

### 16 commit 全列表（时间正序）

| commit | 主题 | 闭环 |
|--------|------|------|
| `c1c529c` | **v17.65** · ChartScene v18 跨周期十字深度（主图 KLineCrosshairView externalTime + SymbolBinding sourcePaneID 排除自身回流） | 跨周期共振 |
| `32be45b` | **v17.66** · Tab Detach 持久化（detachedPaneIDStrings + UserDefaults + willTerminate guard） | 多屏 trader |
| `bf7ce75` | **v17.67** · Workspace 内置预设库（5 个：多周期共振 / 多品种 / 自选+图表 / 期权策略台 / 9 格综合） | Workspace |
| `b949fd7` | **v17.68** · ⌘K 命令面板内容完善（20+ 命令 · 接通 addPaneToActiveWorkspace TODO） | 命令面板 |
| `5de8e19` | **v17.69** · ⌘K 模糊匹配 + 排名 + 搜索高亮（4 级评分 exact/prefix/substring/subsequence） | 命令面板 |
| `86edf53` | **v17.70** · Sidebar alertHistory 第 6 section + cleanup duplicateActiveWorkspace | Sidebar 真数据 |
| `f8cb529` | **v17.71** · ChartScene v19 副图跨周期共振（SubChartView externalTime + 浅蓝虚线） | 跨周期共振 |
| `5cb00d5` | **v17.72** · PaneHeader period inline Menu（12 周期分 3 段 · 同 group 广播） | UX |
| `958866b` | **v17.73** · ⌘K 加 23 个独立窗口快速打开命令 | 命令面板 |
| `f32c423` | **v17.74** · WorkspaceTab 排序（contextMenu ← / →） | Workspace |
| `69e0118` | **v17.75** · simplifier L1 复检 · 应用 2 条简化（setPanePeriod 单边写 / valid 集合二次构造） | 代码质量 |
| `e835462` | **v17.76** · ChartHitTester xPosition(forTime:) 跨 module 统一映射 | 重构 |
| `3e589af` | **v17.77** · ⌘K 命令面板接通 WatchlistBook 真自选合约 | 命令面板 |
| `5918102` | **v17.78** · ⌘K 补齐 polish（toggle BottomBar/Sidebar 折叠 + 新建空白 Workspace） | 命令面板 |
| `582135e` | **v17.79** · 副图本地 hover 同步光标（接续 v17.65/71/76） | 跨周期共振 |
| `b7a3b09` | **v17.80** · 副图本地/跨 Pane 光标分色（本地黄白 [4,4] + 跨 Pane 浅蓝 [2,2]） | 跨周期共振 |

**累计**：**16 文件变更 · +973/-47 行 · 2 新文件 · 测试 2657 全绿**。

---

## 2 · 仓库与环境

| 维度 | 值 |
|------|---|
| Linux 主目录 | `/home/beelink/macos_tmp/macos_futures_trading_v1` |
| Mac 主目录 | `/Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1` |
| 最新 commit | `b7a3b09`（v17.80） |
| origin/main | **已同步 · 0 commits ahead**（本 session 16 次 push 全部成功） |
| 当前分支 | `main` |
| Linux 测试 | **2657 / 413 swift testing 全绿** · 性能 baseline pass |
| Mac 实测 | **本 session 未切机** · 累积 **~298 commit 待验**（v15.96 + v16.5 + v16.27 → v17.80）|
| Mac 编译 | ⏳ 未验 |
| SSH 免密 | Mac → `beelink@vvsvr` 已配 |
| 独立 build path | `/tmp/build_v1730_b1`（避免主 .build 抢锁）|

---

## 3 · 新会话开工指引

### 3.1 Linux 端

```bash
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1   # 增量 1-5s
swift test  --build-path /tmp/build_v1730_b1   # 期待 2657 全绿（~14s · 偶发 flaky 重跑通过）
```

### 3.2 Mac 切机（累积 ~298 commit 待验）

```bash
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                       # 拉本 session 16 commit + 之前累积
./mac_acceptance.sh --skip-shots               # ~1min 快速验
./mac_acceptance.sh                            # ~5min 完整验
```

---

## 4 · 本 session 关键产出（按功能闭环分组）

### 闭环 A · 跨周期十字光标共振（v17.65 / 71 / 76 / 79 / 80）

**v17.65** 主图 KLineCrosshairView 加 `externalTime` 参数 + `externalBarX/CrosshairLine/TopTag` 3 helpers ·
SymbolBinding 加 `crosshairSourcePaneIDString` Optional 字段 · effectiveCrosshair 排除自身回流。

**v17.71** SubChartView 加 `externalTime: Date?` + `externalBarX` helper · ChartContentView 加 `@Environment(\.shellExternalCrosshair)` 透传。

**v17.76** ChartHitTester 加 `xPosition(forTime:in:width:viewport:)` static helper · KLineCrosshairView + SubChartView 的 `externalBarX` 改调跨 module helper · 算法统一在 ChartCore。

**v17.79** ChartContentView 加 `effectiveSubChartCursorTime` · 本地 hover 优先 / shellExternalCrosshair fallback。

**v17.80** 拆分两路：SubChartView 加 `localTime`（黄白 [4,4] · 同主图风格）+ `externalTime`（浅蓝 [2,2] · 跨 Pane）· 两层并存。

**最终视觉**：
- 本图 hover 主图 → 副图黄白虚线（与主图本地光标风格一致）
- 同 group 兄弟图 hover → 副图浅蓝虚线（区分非本图来源）
- trader 一眼区分"我在哪 / 兄弟在哪"

### 闭环 B · Tab Detach 持久化（v17.66）

ShellViewModel 加 `detachedPaneIDStrings` + 4 个 mark/restore 方法 + `isApplicationTerminating` flag。
ShellWindow `.onAppear` 一次性 restore（hasRestoredDetachedWindows 防多 ShellWindow 重复触发） + `.onReceive(willTerminateNotification)` 设 flag。
PaneContainer 📤 按钮调 `markPaneDetached` · DetachedPaneWindow `.onDisappear` 调 `markPaneAttached`（terminate 时跳过保留 list）。

### 闭环 C · Workspace 预设库（v17.67 / 74）

**v17.67** 新文件 `WorkspacePreset.swift` · 5 个内置预设（多周期共振 / 多品种监控 / 自选+图表 / 期权策略台 / 全屏 9 格综合） · 新 `WorkspacePresetPickerSheet.swift` LazyVGrid 2 列卡片 · ShellViewModel 加 `newWorkspace(from preset:)` · WorkspaceTabBar + 按钮改 Menu。

**v17.74** WorkspaceTab contextMenu 加「← 左移 / → 右移」· ShellViewModel 加 `moveWorkspace(_:by:)`（同 PrimaryTab 内 swapAt）。

### 闭环 D · ⌘K 命令面板大改（v17.68 / 69 / 73 / 77 / 78）

**v17.68** 接通 addPaneToActiveWorkspace TODO · 加 20+ 命令（5 预设 / 6 布局 / Pane 最大化 / Inspector / F 键全套 / Workspace 复制/删除）。

**v17.69** 4 级评分搜索：100 exact / 90 prefix / 70 substring / 50 subsequence / 30 subtitle · title.count asc 作 tiebreaker · `highlightedTitle` 命中段 bold + accent 色高亮（Text + Text + Text concat）· subsequence 无连续 range 不高亮。

**v17.73** 加 23 个独立窗口快速打开命令（watchlist/chart/review/alert/journal/trading/training/multichart/formulaEditor/spread/option/sector/heatmap/position/correlation/moneyflow/calendarSpread/instrumentDashboard/sessionCompare/anomalyMonitor/spreadAlert/workspace/backtest）· trader 不记 ⌘⌥X 快捷键。

**v17.77** `@Environment(\.storeManager)` + `@State loadedSymbols` · `.task { await load }` 启动 + 每次打开 ⌘K 刷新 · SQLiteWatchlistBookStore.load() 取 `book.groups.flatMap(\.instrumentIDs)` 去重排序 · 与 mockSymbols 合并去重 · 自选 ⭐ 区分 mock 📊。

**v17.78** 补齐 toggle BottomBar/Sidebar 折叠 + 新建空白 Workspace（v17.67 + 按钮 Menu 已有 · ⌘K 之前漏接）。

### 闭环 E · Sidebar 真数据 + cleanup（v17.70）

SidebarSection 加 `case alertHistory`（emoji 🔔 / displayName "预警"）· SidebarLayoutStore.load 兼容性已存在 · 旧用户配置自动追加新 section。
ShellSidebar 加 `alertHistorySection` 实装 · `@Environment(\.storeManager)` 拿 SQLiteAlertHistoryStore · `.task { await refreshAlertsAsync() }` 启动加载 5 条最近 · UI 显示 🔔 + symbol + alertName + 时间 + triggerPrice · message 走 help tooltip。
顺手 cleanup：v17.68 多余的 `duplicateActiveWorkspace()` 删除（`duplicateWorkspace(_:UUID)` 已存在 · 命令面板改调老方法）。

### 闭环 F · PaneHeader period inline 切换（v17.72）

ShellViewModel 加 `setPanePeriod(paneID:periodRaw:)` · 与 `cyclePeriodOnActivePane` 共享 group 广播逻辑。
PaneContainer.PaneHeader · period 文字改 Menu（仅 chart Pane · 其他 kind 仍纯文字）· 分 3 段：分钟（1m/3m/5m/15m/30m）/ 小时（1h/2h/4h）/ 日及以上（D/W/M/Q/HY/Y）· 当前周期前 ✓ 标记。
PaneContainer 加 `import Shared`（KLinePeriod 来自 Shared.Models.KLine）。

### 闭环 G · simplifier L1 复检（v17.75）

跑 v17.65-74 累积新逻辑 · 报 3 条建议 · 应用 2 条：

1. `setPanePeriod` 双重写改单边分支（有 group 走广播 · 否则单点写 · 删除幂等冗余）
2. `validDetachedPaneIDsForRestore` valid 集合二次构造去除（直接用 `valid.map(\.uuidString)` 作为最终列表）

跳过 1 条：跨 module externalBarX 重复 · 后在 v17.76 重构（提到 ChartHitTester）。

---

## 5 · Mac 切机重点验证清单

累积 **~298 commit 待验**（v15.96 + v16.5 + v16.27 + v17.x → v17.80）

### 🔴 高优先（v17.65-80 新功能 · 影响视觉/交互）

**跨周期十字光标**（v17.65/71/76/79/80）：
- 主图本地 hover 主图 → 主图黄白十字 + 副图黄白虚线（**同色** · 与主图风格一致）
- 同 group 兄弟图 hover → 主图浅蓝虚线 + 顶部 🎯 tag + 副图浅蓝虚线（**区分本地**）
- 跨周期 bar 映射（1m 主图 hover · 5m 副图找包含该时刻的 bar · 颜色一致）

**Tab Detach 持久化**（v17.66）：
- 📤 按钮 detach → 关 App → 重启 App → detached 窗口**自动重新打开**
- 在 detached 窗口手动 ⌘W 关闭 → 重启 App → 不再恢复
- 删除 Pane 后重启 → 自动从 detached list 清掉

**Workspace 预设**（v17.67）：
- WorkspaceTabBar + 按钮 Menu「空白」/「从预设新建」5 项 / 「全部预设…」打开 sheet
- 选「📈 多周期共振」→ 4-Pane 2×2 · rb2510 的 1m/5m/15m/1H · 全 blue group 联动
- 选「🌃 多品种监控」→ 6-Pane 2×3 · 6 主力合约 5m

**⌘K 命令面板**（v17.68/69/73/77/78）：
- 打开 ⌘K · 空 query → 显示"最近用过"+ 常用前 20 条
- 输入 "rb" → 命中 "rb2510" + 含 rb 的所有合约（命中字符 bold + accent 色）
- 输入 "新建" → 新建 Pane / Workspace / 预设 等
- 输入 "F8" → 命中 F 键全套
- 搜 trader 自选合约（从 WatchlistBook 加载） · ⭐ 区分
- 23 个独立窗口快速打开（搜 "预警" / "回测" / "套利"）
- toggle BottomBar/Sidebar 折叠 / Inspector

**Sidebar alertHistory**（v17.70）：
- 默认显示第 6 section（追加在末尾）· 🔔 emoji · 5 条最近触发
- 右键「📋 自定义 section…」可调顺序/显隐 · sheet 含 alertHistory

**PaneHeader period 切换**（v17.72）：
- chart Pane Header 的周期文字 → 点击展开 Menu · 3 段（分钟/小时/日及以上）
- 同 group Pane 自动跟随周期切换
- 当前周期前 ✓ 标记

**WorkspaceTab 排序**（v17.74）：
- 右键 Tab · 「← 左移 / → 右移」· 边界自动 disabled
- 同 PrimaryTab 内移动 · 跨 tab 不影响

### 🟡 中优先（Linux 编译跳过的 macOS API 风险）

- `NSApplication.willTerminateNotification`（ShellWindow v17.66）
- `.menuStyle(.borderlessButton)` + `.menuIndicator(.hidden)` + `.fixedSize()`（v17.67/72 macOS 13+ 已确认 · 但样式可能与系统主题冲突）
- 命令面板 LazyVStack 在 ScrollView 内的滚动性能（v17.68/69 加了 100+ 命令）
- AttributedText concat（`Text(pre) + Text(mid).bold() + Text(post)` · v17.69）

### 🟢 低优先（不影响功能）

- 视觉间距、字体大小、HUD 颜色一致性
- mock 数据扩展性

---

## 6 · 累积变更风险评估

| 维度 | 状态 |
|------|---|
| commit 数量 | ~298 commit 待 Mac 验（v15.96 + v16.5 + v16.27 + v17.x → v17.80） |
| 文件覆盖 | 本 session 16 文件 · 累积大量 Shell 模块 + 2 跨 module（ChartCore/MainApp）|
| 风险面 | Shell module 改动密集 · Mac 切机时若 build 失败 · 用 `git bisect` 范围 v17.65-80 定位 |
| 已知 flaky | `ReplayDriver "跑到末尾自动停"` 偶发失败 · 重跑通过 · 不动 |
| 已知 Linux 编译盲区 | 命令面板 LazyVStack 滚动 / NSApp Notification / `.menuStyle/.menuIndicator/.fixedSize` 视觉对齐（Linux 不验） |

---

## 7 · 剩余 3 项门槛（不变）

| 项 | 工作量 | 状态 |
|---|---|---|
| #6 Pane 自由拖拽切分（custom layout）| 2-3d | **明确推迟**（差距清单 §3.2 详细记录 · 推迟理由：基线未 Mac 实测 · branch 隔离保护打折 · 7 预设覆盖 90% trader 用例） |
| #11 A1.4 Hollow/Bars OHLC 10w bars 性能 | Mac 验证 | 需 Mac 实测 · Linux 无法测 |
| #12 A6.2 CloudKit CKContainer 真接 | M7+ | 需 Apple 设备 |

---

## 8 · 下一步候选（按 ROI 排序）

### 🥇 Mac 切机验收（**强烈推荐**）

累积 ~298 commit · v17.65-80 的视觉改动密集 · **必须切机看实际效果**：
- 跨周期十字光标本地/跨 Pane 双色显示
- Tab Detach 重启恢复
- 命令面板搜索 + 高亮
- Workspace 预设卡片视觉

切机流程见 §3.2。

### 🥈 #6 Pane 自由拖拽切分（custom layout）

Mac 验收稳定后再开 branch · 见差距清单 §3.2 实装方案备忘：
- `enum PaneLayout` → `indirect enum PaneNode { case leaf / hSplit / vSplit }` tree
- AppKit `NSSplitView` + delegate · `NSViewRepresentable` 桥接
- 拖拽 Pane 标题栏 · 落点检测 · 重组树
- 递归 Codable 树结构

### 🥉 探索其他方向

- ChartScene v20+ polish（HUD / Sheet 样式微调）
- Sidebar Anomaly 30s mock pool 模拟（数据真实化）
- 麦语言新函数补完（已 355 个 · 远超 50 目标）
- D 段公式回测 v3 增强

---

## 9 · 一行命令速查

```bash
# Linux 主开发
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1     # 1-5s 增量
swift test  --build-path /tmp/build_v1730_b1     # 期待 2657 全绿

# Mac 切机
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                          # 拉本 session 16 commit
./mac_acceptance.sh --skip-shots                  # ~1min
./mac_acceptance.sh                               # ~5min

# 看本 session 全 commit
git log --oneline c394556^..HEAD                  # 16 commit
git diff c394556..HEAD --stat                     # 16 文件 +973/-47
```

---

## 10 · 启动新会话推荐（直接复制）

```text
项目：中国期货 Mac/iPad 原生工作台 · v17.80 · Shell ~99.5% + TradingView 99% · 16 commit 已 push

【必读交接文件】
/home/beelink/macos_tmp/macos_futures_trading_v1/会话交接-2026-05-11-v17.80-Shell99.5%+16commit跨周期+预设+⌘K大改+副图分色.md

【必读差距清单】
/home/beelink/macos_tmp/macos_futures_trading_v1/差距清单与后续路线-v17.52后.md

【一句话状态】
v17.65 → v17.80 · 16 commit 全部已 push · +973/-47 行 · 测试 2657 全绿 16/16 · Shell 完整度 95% → ~99.5% · v17.0 §13.2 设计 7 项完成 6/7

【本 session 7 大功能闭环】
- 🎯 跨周期十字光标共振（主图 v17.65 + 副图 v17.71 + 统一 v17.76 + 双色 v17.79/80）
- 🎯 Tab Detach 持久化 v17.66（UserDefaults + willTerminate guard）
- 🎯 Workspace 预设库 v17.67（5 个）+ 排序 v17.74
- 🎯 ⌘K 命令面板大改 v17.68/69/73/77/78（20+5+23+真自选+折叠 polish · 4 级评分 + 高亮）
- 🎯 Sidebar alertHistory 第 6 section v17.70（真数据）
- 🎯 PaneHeader period inline Menu v17.72
- 🎯 simplifier L1 复检 v17.75 + ChartHitTester 跨 module 统一 v17.76

【Mac 切机状态】
- ⏳ 累积 ~298 commit（v15.96 + v16.5 + v16.27 + v17.x → v17.80）
- 🔴 重点验证：
  · 跨周期光标本地黄白 / 跨 Pane 浅蓝 双色
  · Tab Detach 重启自动恢复
  · ⌘K 搜索高亮 + 23 窗口快速打开 + WatchlistBook 真自选
  · Workspace 预设 5 个（PickerSheet 卡片视图）
  · Sidebar alertHistory 第 6 section
  · PaneHeader period inline Menu（chart Pane）
  · WorkspaceTab 右键左右移

【剩余 3 项门槛】
- #6 Pane 自由拖拽切分（2-3d · 明确推迟 · 差距清单 §3.2 详细方案备忘）
- #11 A1.4 Hollow/Bars 10w bars 性能 Mac 实测
- #12 A6.2 CloudKit CKContainer 真接（M7+ Apple 设备）

【下一步候选】
🥇 Mac 切机验收（累积 ~298 commit · 强烈推荐）
🥈 #6 Pane 自由拖拽切分 custom layout（Shell 99.5% → 100%）
🥉 ChartScene v20+ 探索 / 其他方向

【关键提醒】
- 16 commit 全部已 push origin · 无积压
- ReplayDriver "跑到末尾自动停" 偶发 flaky · 重跑通过 · 不动
- ShellViewModel 在 App 级 @StateObject · 跨 detached 窗口共享（含 v17.66 hasRestoredDetachedWindows guard）
- 本地 hover 时副图黄白（与主图一致）· 跨 Pane 兄弟广播时副图浅蓝（v17.80 视觉区分）
- 命令面板 LazyVStack ScrollView 滚动 + AttributedText concat 高亮 · Linux 编译跳过 macOS UI 性能盲区
- ⌘K 命令面板内容 ~120+ 条（5 PrimaryTab + N Workspace + N symbols + 6 Pane 新建 + 5 预设 + 6 布局 + 23 窗口 + F 键 + Workspace 操作 + 主题 + Inspector/BottomBar/Sidebar toggle）
- 独立 build path = /tmp/build_v1730_b1

请先读交接文件 · 然后回报"已了解" + 等下一步指令。
```
