# 会话交接 v17.131 → v17.138 · 自选管理深化 batch IV-VII 全收口 + fib 7 件套收口 + Workspace 预设导入导出 + 主图时间范围预设 · 7 commit 已 push · 2026-05-12

## 1 · 项目极简（30 秒了解）

**做什么**：中国期货 Mac/iPad 原生专业工作台 · Stage A 不接 CTP 下单
**关键时点**：M6（2026-09 末）= Pro 订阅上线 = 生死节点
**本 session 核心**（共 **7 commit · 7 语义化版本（v17.132 - v17.138）· 全部已 push**）：

接续 v17.131 交接 · 一气呵成 3 个大块：

1. **自选管理深化 batch IV-VII 全收口**（v17.132-135 · 4 commit）：多标签 + 标签筛选 / 置顶合约 ≤ 3 / 别名 + CSV 扩列 / 批量备注 → 自选管理深化系列 batch I-VII 完整收口
2. **fib 7 件套收口**（v17.136 · 1 commit · 第 7 个画线收尾）：fibonacciSpiral 黄金螺旋 · DrawingType 27 → 28 · memory `feedback_战略不补` 中"fibonacciSpiral 实战派少用"作废
3. **新方向探索**（v17.137-138 · 2 commit）：Workspace 用户预设 export/import JSON + 主图时间范围预设 ⌘⌥1-6

**测试 2704 → 2739 全绿**（Linux +35 新单测：9 tag + 10 pinned + 9 alias + 7 timeRange · macOS 实跑 +10 MainAppTests）。

### 7 commit 全列表（时间正序）

| commit | 主题 | 备注 |
|--------|------|------|
| `e0b0f0b` | **v17.132** · 加合约多标签 InstrumentTagStore + 标签筛选 | §4.A 自选 batch IV |
| `5b893df` | **v17.133** · 加自选置顶合约 pinnedInstrumentIDs（每组 ≤ 3） | §4.B 自选 batch V |
| `6b94918` | **v17.134** · 加自选合约别名 InstrumentAliasStore | §4.C 自选 batch VI · CSV 8 列 |
| `f5bfcc0` | **v17.135** · 加批量备注（多选 N 个合约写同一备注） | §4.D 自选 batch VII 收口 |
| `f703133` | **v17.136** · 加 fibonacciSpiral 黄金螺旋 | §4.E 画线 27→28 · fib 7 件套收口 |
| `ac07b92` | **v17.137** · Workspace 用户预设 export/import JSON 文件 | §4.F 新方向 · Shell 层 |
| `c35df2e` | **v17.138** · 加主图时间范围预设 ⌘⌥1-6 = 1D/1W/1M/3M/6M/1Y | §4.G 新方向 · UI |

**累计**：**17 文件变更 · +1727/-18 行 · 35 新 Linux 单测 + 10 macOS-only 单测 · 测试 2739 全绿**。

---

## 2 · 仓库与环境

| 维度 | 值 |
|------|---|
| Linux 主目录 | `/home/beelink/macos_tmp/macos_futures_trading_v1` |
| Mac 主目录 | `/Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1` |
| 最新 commit | `c35df2e`（v17.138 · 时间范围预设） |
| origin/main | **已同步 · 0 commits ahead**（本 session 7 次 push 全部成功） |
| 当前分支 | `main` |
| Linux 测试 | **2739 / 422 swift testing 全绿** · 性能 baseline pass · ReplayDriver flaky 偶发重跑通过 |
| Mac 实测 | **本 session 未切机** · 累积 **~368 commit 待验**（含 v17.132-138 + v17.118-131 + 之前 ~347） |
| Mac 编译 | ⏳ 未验 |
| SSH 免密 | Mac → `beelink@vvsvr` 已配 |
| 独立 build path | `/tmp/build_v1730_b1`（避免主 .build 抢锁） |

### 代码量（本 session 末快照）

| 维度 | 数量 |
|---|---|
| **总 swift 文件数** | **421**（+4：InstrumentTag / InstrumentAlias / UserWorkspacePresetTransfer / ChartTimeRangePreset）|
| Sources 文件 | 421 / ~101,800 行 |
| 单文件最大 | ChartScene.swift（~5,310 行 · 本 session +25 行：fib 螺旋 6 处接通 + 时间预设 ⌘⌥1-6） |
| 第二大 | WatchlistWindow.swift（~2,820 行 · 本 session +526 行：标签 / 置顶 / 别名 / 批量备注） |

---

## 3 · 新会话开工指引

### 3.1 Linux 端

```bash
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1   # 增量 1-5s
swift test  --build-path /tmp/build_v1730_b1   # 期待 2739 全绿（~14s · ReplayDriver flaky 偶发重跑通过）
```

### 3.2 Mac 切机（累积 ~368 commit 待验）

```bash
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                       # 拉本 session 7 commit + 之前累积
./mac_acceptance.sh --skip-shots               # ~1min 快速验
./mac_acceptance.sh                            # ~5min 完整验
```

**MainAppTests 验证（macOS 实跑）**：
```bash
swift test --build-path /tmp/build_v1730_b1 --filter "UserWorkspacePresetTransferTests"
# 期待 10 单测全绿（v17.137 · Linux 0 tests · macOS 实跑 Transfer 容器 + ShellViewModel API）
swift test --build-path /tmp/build_v1730_b1 --filter "ChartThemeColorTests"
# 期待 9 单测全绿（v17.124 已有）
```

---

## 4 · 本 session 关键产出

### A · v17.132 · 自选合约多标签 + 标签筛选（batch IV）

**Why**：trader 实战分类标记 · 「主力 / 次主力 / 套利腿 / 对冲腿」/「日内 / 波段 / 长线」/ 板块标签。

**Shared/InstrumentTagStore**：
- UserDefaults `[String: [String]]`（instrumentID → tag 数组）· 与 InstrumentFlag/Note 同模式
- 单合约 ≤ 10 标签 · 单标签 ≤ 20 字 · trim / 去重 / 上限截断
- API：tags / setTags / addTag / removeTag / hasTag / hasTags / allInstrumentTags / allTagsAcrossInstruments / clearAll

**WatchlistWindow UI**：
- 行 🏷️N 小标识 · hover tooltip 列全部标签
- 单合约右键「🏷️ 添加 / 标签管理 / 清空」
- 多选右键「🏷️ 批量加标签」(从已有标签选)
- toolbar 加 tagFilterMenu（多选 OR 关系 · 跨重启保留 · `viewState.v1.watchlist.tagFilterRaw` AppStorage CSV）
- TagEditSheet（520×460 · 已选 chip + 输入框 + 已有标签建议）

**+9 单测**：默认空 / set 往返 + 空数组移除 / addTag 去重 + 返回新增 / removeTag · trim 后匹配 / sanitize 三重 / 多 instrument 独立 / allTagsAcrossInstruments 去重排序 / 上限保护 / clearAll 跨实例。

### B · v17.133 · 自选置顶合约 ≤ 3（batch V）

**Why**：trader 主力合约（RB0/AU0/IF0）永远在分组顶部 · 不被涨跌幅排序冲散。

**Shared/Watchlist 模型扩**：
- 加 `pinnedInstrumentIDs: [String]?` · 默认 nil · 兼容旧 JSON
- `Watchlist.maxPinnedPerGroup = 3` 公共常量
- WatchlistBook 加 `pinInstrument` / `unpinInstrument` / `isPinned`（mutating + 幂等 + 上限拒绝）
- `removeInstrument` 同步清掉置顶引用（防 stale）
- 跨组 `moveInstrument` 清源组置顶 · 目标组不携带

**WatchlistWindow UI**：
- `sortedInstrumentIDs(for:)` 加 pinned 分段（pinned 按 pin 顺序在前 · 其余按 sortField 排）
- 行 📌 标识（橙色 45° 旋转 pin.fill · 视觉锚点）
- 单合约右键加「📌 置顶到组顶部 / 取消置顶」· 已满 3/3 disabled

**+10 单测**：默认 nil / 写入读回 + isPinned / 不存在合约拒绝 / 幂等 + version 不 bump / 上限 3 / unpin 空数组 nil / removeInstrument 清 stale / 跨组 move 清源 / Codable 往返 / 旧 JSON 兼容。

### C · v17.134 · 自选合约别名 + CSV 扩列（batch VI）

**Why**：「m2509」→「豆粕 0509」/「RB0」→「螺纹主力」· trader 自定义中文可读名。

**Shared/InstrumentAliasStore**：
- UserDefaults `[String: String]` · 单别名 ≤ 20 字 · trim / 截断
- `displayName(for:)` 工具：有别名返回 "alias (id)" · 无返回 id

**WatchlistWindow UI**：
- 合约行用 alias 替代 ID 显示（alias 时切到非 monospaced 字体）· tooltip 含原 ID
- 右键加「✏️ 设别名 / 编辑别名 / 清除别名」
- AliasEditSheet（460×200 · 单行输入 + 字符计数）
- **CSV 导出扩列**：6 → 8 列（合约 + **别名** + 最新价 + 涨跌幅 + 持仓量 + 旗标 + **标签** + 备注 · trader 复盘材料完整化）

**+9 单测**：默认 nil / set 往返 / 空白/nil 移除 / trim / 超长截断 / displayName 格式 / 多 instrument 独立 / clearAll / 跨实例共享。

### D · v17.135 · 批量备注（batch VII 收口）

**Why**：套利组「豆系套利组 · 11-1 价差扩大」一次给 N 个合约写同一备注。

**WatchlistWindow UI**（纯 UI · 无 Shared 改动）：
- 多选 contextMenu 加「📝 批量加备注 N 个 →」(仅 ids.count ≥ 2)
- BatchNoteEditSheet（500×380 · TextEditor + 受影响合约列表 prefix 10 + "等 N 个" + 字数统计）
- 应用 → 遍历 setNote · 覆盖原备注 · 留空清除所有 N 个
- BatchNoteTarget Identifiable 包装（sorted-join 作 sheet id · 变化即重建）

**至此自选管理深化 batch I-VII 完整收口**：
- I  v17.129 合约备注 InstrumentNoteStore
- II v17.130 批量移动 / 批量旗标 / 导出 CSV
- III v17.131 每组独立排序
- IV  v17.132 多标签 + 标签筛选
- V   v17.133 置顶合约 ≤ 3
- VI  v17.134 别名 + CSV 加别名/标签列
- VII v17.135 批量备注 ← 收尾

### E · v17.136 · fibonacciSpiral 黄金螺旋（fib 7 件套收口 · 27 → 28）

**Memory 决策反转**：`feedback_战略不补.md` 原标"fibonacciSpiral 实战派少用"作废 · 用户主动要求收口。

**实装**（6 处接通 · 同 commit 修测试期望 · 防 v17.131 漏改复发）：
1. Shared/Drawing.swift · 加 `.fibonacciSpiral` case + pointsNeeded == 2 + factory
2. MainApp/ChartScene.swift · displayName / suggestCategory(.channel) / toolbar(tornado icon) / hover preview
3. ChartCore/DrawingsOverlayView.swift · 渲染 dispatch + `drawFibonacciSpiral` + 金黄配色
   - 算法：中心 = A · 起始半径 = ||AB|| · 5 段 1/4 弧按 fib 数列 1/1/2/3/5/8 半径扩展
   - 每段弧首尾相切（弧心沿 segEnd 方向位移 prevR · 视觉连续螺旋）
   - 弧中点标 fib 数 · 中心点小圆标识
4. Tests/SharedTests/DrawingsTests/DrawingsTests.swift · "27 种" → "28 种" 三处 · coveredCount 19 → 20 · factoryTypes + pointsNeededContract + roundTrip 都加 fibonacciSpiral entry

**至此 fib 7 件套完整**：fibonacci / fibonacciExtension / fibonacciArc / fibonacciChannel / fibonacciFan / fibonacciTimeZone / fibonacciSpiral。

### F · v17.137 · Workspace 用户预设 export/import JSON（新方向 · Shell 层）

**Why**：trader 备份精心调好的预设（防误删 · 重装恢复）· 跨机迁移（公司/家里）· 分享给同事或社区。

**新增 4 文件**：
- `Sources/MainApp/Shell/UserWorkspacePresetTransfer.swift`：
  - 带 `schemaVersion` 容器（v1 · 防未来格式破坏 · decodeIfPresent fallback）
  - `WorkspacePresetImportMode`（append / replaceAll）
  - `WorkspacePresetImportError`（fileEmpty / invalidJSON / unsupportedSchemaVersion）
- `ShellViewModel` 加：
  - `exportUserPresets() -> Data?`（prettyPrinted + sortedKeys 利于 diff）
  - `importUserPresets(data:, mode:) throws -> WorkspacePresetImportResult`
  - 导入时全部**重新生成 UUID** 防 id 碰撞（与现有 / 文件内）
- `WorkspacePresetPickerSheet` footer 加按钮：
  - 「📤 导出全部 (N)」NSSavePanel · 文件名 `workspace_presets_YYYYMMDD.json`
  - 「📥 导入...」NSOpenPanel · 已有预设时 NSAlert 询问 append / replaceAll / 取消
  - 完整错误反馈（Toast.warn 具体原因）
- `Tests/MainAppTests/UserWorkspacePresetTransferTests.swift`：10 单测（Transfer round-trip + 旧 JSON 兼容 + 8 ShellViewModel API）· `#if guard` macOS 实跑 · Linux 0 tests

### G · v17.138 · 主图时间范围预设 ⌘⌥1-6 = 1D/1W/1M/3M/6M/1Y（新方向 · UI）

**Why**：trader 复盘高频 · 「⌘⌥2 一键切到最近 1 周」「⌘⌥3 月范围」「⌘⌥4 季度趋势」。

**Shared/ChartTimeRangePreset**：
- 6 case · 国际惯例命名 1D/1W/1M/3M/6M/1Y
- 按 KLinePeriod ceil 计算所需 bars 数（向上取整 · 至少 10）
  - 1D × 5m = 288 bars · 1W × 1h = 168 · 1M × daily = 30 · 1Y × daily = 365
- period > 范围（如 1D × weekly）clamp 到 10 bars
- 纯数据 · 跨端可移植

**ChartScene UI**：
- viewportShortcuts 加 6 个 ⌘⌥N 隐藏按钮（ForEach over enumerated allCases）
- `applyTimeRangePreset` 计算 visibleCount = min(want, bars.count) · clamp 防越界
- presentToggleNotice 反馈"时间范围：1W（N 根）"
- ⚠️ 暂无可见按钮 UI（trader 用 ⌘/ 快捷键 help 查 · 也可后批加 toolbar 按钮组）

**+7 单测**：displayNames / seconds 单调递增 / 1D × 5m / 1W × 1h / 月年 × daily / period > 范围 clamp / ceil 不丢半根。

---

## 5 · Mac 切机重点验证清单

累积 **~368 commit 待验**（含 v17.132-138）

### 🔴 高优先（本 session 新功能）

**v17.132 多标签 + 筛选**：
- 自选合约右键 → 🏷️ 添加标签 sheet · 已有标签建议（chip）· 上限 10 个
- 行显示 🏷️N 小标识 + hover tooltip 列全部
- toolbar tagFilterMenu 多选 OR 关系 · 选「主力」+ 「日内」显含任一的合约
- 多选 ≥ 2 时右键「🏷️ 批量加标签」从已有标签下拉

**v17.133 置顶合约**：
- 单合约右键「📌 置顶到组顶部」· 行显 📌（橙色 45° pin.fill）
- 按涨幅排序时 pinned 仍在最顶部
- 已置顶 3 个再 pin 第 4 个 → 菜单 disabled
- 拖拽该合约出组（跨组移动）→ pin 清除（重新决策语义）
- 删合约 → pin 同步清除

**v17.134 别名 + CSV 扩列**：
- 右键 ✏️ 设别名「螺纹主力」→ 行主显「螺纹主力」非 mono 字体 · tooltip「螺纹主力（RB0）」
- 切「紧凑」字号 → alias 跟字号变化
- 分组右键导出 CSV → 用 Excel 打开看 8 列（合约 / 别名 / 最新价 / 涨跌幅 / 持仓量 / 旗标 / 标签 / 备注）· 中文不乱码

**v17.135 批量备注**：
- 自选选 2+ 合约 → 右键「📝 批量加备注 N 个 →」sheet
- sheet 上方 chip 列出前 10 个合约 · 超 10 显「等 N 个」
- 写「豆系套利组」点应用 → 所有选中合约的备注都变这个

**v17.136 fibonacciSpiral**：
- 工具栏第 3 组（斐波系列）加 tornado icon · hover 显「斐波那契螺旋 / 黄金螺旋」
- 两点拖拽 → 5 段 1/4 弧螺旋扩展 · 每弧 fib 数 1/1/2/3/5/8 标号
- 中心点小圆标识
- 金黄配色（与 fibonacciExtension 协调）

**v17.137 Workspace 导入导出**：
- ⌘K 命令面板「新建 workspace · 预设」打开 WorkspacePresetPickerSheet
- 底部「📤 导出全部 (N)」NSSavePanel → 保存 workspace_presets_YYYYMMDD.json
- 「📥 导入...」NSOpenPanel → 已有预设时弹 NSAlert 选追加/替换/取消
- Toast 反馈具体导入数量 / 错误原因

**v17.138 时间范围预设**：
- ⌘⌥1 → 切到最近 1D（按当前 period bars 数）· HUD 显「时间范围：1D（N 根）」
- ⌘⌥2/3/4/5/6 → 1W/1M/3M/6M/1Y
- bars 不足时 visibleCount clamp 到 bars.count（不报错）

### 🟡 中优先（Linux 编译跳过的 macOS 风险）

- Watchlist Codable 新字段 `pinnedInstrumentIDs` 旧 JSON 兼容（已单测）
- ShellViewModel @MainActor + UserDefaults 持久化 race
- NSAlert / NSSavePanel / NSOpenPanel macOS 实际 panel 行为
- ⌘⌥1-6 跟其他全局快捷键防冲突（grep 已确认无冲突）

### 🟢 低优先

- TagEditSheet 中文输入法兼容
- AliasEditSheet TextField 单行输入习惯
- CSV BOM 在 Excel for Mac 中文识别（应该正常 · 备 fallback Numbers）

---

## 6 · 累积变更风险评估

| 维度 | 状态 |
|------|---|
| commit 数量 | ~368 commit 待 Mac 验（含本 session 7） |
| 文件覆盖 | 本 session 17 文件 · 主要 MainApp（5 文件）+ Shared（4 文件 NEW + 1 改）+ ChartCore（1 文件）+ Tests（6 文件 · 4 NEW）|
| 风险面 | Watchlist 模型扩 `pinnedInstrumentIDs` 字段（decodeIfPresent 守 · 测试覆盖旧 JSON）· DrawingType 加 case（跨 module enum · build 验 exhaustiveness）· ShellViewModel @MainActor mutating 加 export/import · NSAlert blocking call macOS-only |
| 已知 flaky | `ReplayDriver "跑到末尾自动停"` 偶发失败 · 重跑通过 · 不动 |
| 已知 Linux 编译盲区 | UserWorkspacePresetTransferTests 跨 executableTarget · Linux 跳过 · macOS 首次切机时实跑验证 |

---

## 7 · 剩余推迟项 / Stage B（不变）

| 项 | 工作量 | 状态 |
|---|---|---|
| #6 Pane 自由拖拽切分（custom layout） | 2-3d | **明确推迟** |
| #11 A1.4 Hollow/Bars OHLC 10w bars 性能 | Mac 验证 | 需 Mac 实测 |
| #12 A6.2 CloudKit CKContainer 真接 | M7+ | 需 Apple 设备 |
| MoneyFlow / Position Window 真持仓数据 | CTP 接入后 | CTP 用户已搁置 |
| Stubs.swift 订阅 tab | Stage B | 用户搁置（IAP） |
| 数据库加密 passphrase Keychain wire | Stage B | 留 IAP 引导 |

---

## 8 · 三方向待做清单（v17.138 末 · 用户问明确）

### A · 已有窗口的体验深化 · 5-6 项 / ~2-4d

| 已做 ✅ | 待做 🤔 |
|---|---|
| tooltip 全工程 **314 处** | 🥇 全局快捷键速查表（仅 ChartScene ⌘/ help · 21 窗口未统一）|
| keyboardShortcut **291 处** | 🥈 sidebar 紧凑/标准/宽松密度切换（小屏自适应）|
| ChartScene ⌘/ help | 🥉 各窗口 tooltip audit 补全 |
| | 4️⃣ 工具栏按钮自定义 |
| | 5️⃣ 窗口锁定 ⌘W 不响应 |
| | 6️⃣ ⌘C 行为统一 |

### B · 新副图指标补充 · 6 项 / ~2-3d

现有 **22 个**副图（MACD/KDJ/RSI/Volume/OBV/CCI/WR/OI/DMI/Stoch/ROC/BIAS/Aroon/STC/ElderRay/Chop/FI/BBW/ATRP/VolumeProfile）

| 类型 | 待做 | 工作量 |
|---|---|---|
| **主图 overlay（需扩 MA/BOLL 框架 · 一次性 0.5d 投入）** | 🥇 **VWAP**（trader 日内基准）· 🥈 **Pivot Points**（短线支撑阻力）· 🥉 **SuperTrend**（ATR 趋势）| 0.5-1d/个 |
| **副图（接现有框架）** | **Ichimoku**（一目均衡表）· TRIX · CMF | 0.3-0.5d/个 |

### C · 自选合约管理深化 · 已基本饱和 · 剩 3-4 项 / ~1.5-2d

已做（13 项）：✅ 多 group · ✅ 每组独立排序 · ✅ 旗标 · ✅ 备注 · ✅ 多标签+筛选 · ✅ 置顶≤3 · ✅ 别名 · ✅ 批量备注 · ✅ 批量旗标/移动/加标签 · ✅ 颜色 · ✅ CSV · ✅ CloudKit 预埋 · ✅ 软删除

| 待做（边际递减）| 工作量 |
|---|---|
| 🥇 标签批量管理（global rename / merge tags） | 0.3d |
| 🥈 自选 archive 档案（不删隐藏 · 已平仓存档） | 0.3d |
| 🥉 多 group 嵌套（父子 "黑色 / 螺纹"） | 0.5-1d |
| 4️⃣ 智能分组（rule-based · 涨幅 ≥ 5% 自动入暴涨组） | 1d |
| 5️⃣ 标签颜色 / 拖拽排序 | 0.3d |

---

## 9 · 下一会话候选 · 强推 🥇

### 🥇 主图 overlay 框架 + VWAP（最 ROI · trader 直接价格判断需求）

**最该做**：
1. 扩 ChartIndicatorRunner 主图 overlay 框架（一次性 ~0.5d · 之后每加一个指标 0.2d）
2. VWAP（trader 日内基准线 · 最常用）
3. 顺手 Pivot Points + SuperTrend（同框架）

总 ~1.5d 干完 3 个主图 overlay 指标。

### 🥈 Ichimoku 副图（接现有框架 · 0.5d 单 batch）

国际派常用 · 5 线（转换/基准/先行A/先行B/迟行）· 全自动云图。

### 🥉 全局快捷键速查表（窗口体验类）

21 个窗口加统一 ⌘/ help · sidebar 改 collapsible · ~0.5d。

### 4️⃣ 自选管理收尾（边际递减）

- 标签批量管理（rename / merge）· 0.3d
- 自选 archive 档案 · 0.3d

### 5️⃣ Mac 切机验收（累积 ~368 commit · 用户主动说才做）

---

## 10 · 一行命令速查

```bash
# Linux 主开发
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1     # 1-5s 增量
swift test  --build-path /tmp/build_v1730_b1     # 期待 2739 全绿

# Mac 切机
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                          # 拉本 session 7 commit
./mac_acceptance.sh --skip-shots                  # ~1min
./mac_acceptance.sh                               # ~5min

# 看本 session 全 commit
git log --oneline 0e318ff..HEAD                  # 7 commit
git diff 0e318ff..HEAD --stat                    # 17 文件 +1727/-18

# macOS 实跑（v17.137 Workspace 导入导出）
swift test --build-path /tmp/build_v1730_b1 --filter "UserWorkspacePresetTransferTests"

# macOS 实跑（v17.124 ChartTheme PnL · 已有）
swift test --build-path /tmp/build_v1730_b1 --filter "ChartThemeColorTests"
```

---

## 11 · 本 session 学到的（写入下次会话提醒）

### Memory 决策反转：feedback_战略不补 中"fibonacciSpiral 实战派少用"作废

v17.136 用户主动要求收尾 fib 7 件套 · `D3-风险与危机预案.md` §4 战略不补表中"fibonacciSpiral"明确推翻。

**经验**：用户主动要求 strategic 补完时（强迫症收口 / 完整闭环），不要拿"memory 说不做"挡回去。先确认意图再开做（同 v17.128 艾略特决策模式）。

### DrawingType 加 case 同 commit 修测试期望（v17.136 严格遵守）

v17.131 漏改的教训已经吸取 · v17.136 严格同 commit 改：
- "27 种" → "28 种" 三处（factoryTypes / pointsNeededContract / roundTrip 三个 Suite docstring）
- coveredCount 19 → 20
- factoryTypes 加 entry
- pointsNeededContract 加 entry
- roundTrip 加 entry

下次加新 DrawingType 必须**同 commit** 改 DrawingsTests 全部 4 处。

### Watchlist 加字段 decodeIfPresent + 旧 JSON 单测兼容（v17.133 模板）

v17.133 加 `pinnedInstrumentIDs: [String]?` · 严格遵守 v17.131 模板：
- struct 字段用 `var x: T?`
- CodingKeys 加 case
- decode 用 `decodeIfPresent`
- encode 用 `encodeIfPresent`
- 单测覆盖旧 JSON decode 兼容（用字符串构造模拟旧 JSON 直接 decode）
- removeInstrument / 跨组 move 等关联 mutation 同步清理 stale 引用

### #expect 不能直接调 mutating 方法（v17.133 再次踩坑）

`#expect(book.pinInstrument(...))` 编译错（macro 把 book 当 immutable）。
**解决**：先 `let ok = book.pinInstrument(...)` 然后 `#expect(ok)`。
**已在 v17.131 memory 记录 · v17.133 第二次踩坑** · 写新单测时需要先想到。

### MainAppTests target @MainActor 单测（v17.137 新 pattern）

`ShellViewModel` 是 `@MainActor` · 测试 Suite 也要 `@MainActor` 标记：

```swift
@Suite("ShellViewModel · v17.137 export/import 用户预设")
@MainActor
struct ShellViewModelExportImportTests { ... }
```

否则 swift-testing 调 mutating 方法时报"Main actor isolated"错。

### NSAlert button order 的取消语义（v17.137）

`addButton(withTitle: "取消")` 第 3 个 button 用户点击返回 `.alertThirdButtonReturn` · 不能 fallthrough 当 append 处理。返回 `Optional<Mode>` · 取消返回 nil · 调用方 `guard let mode else { return }`。

### swift-testing filter Chinese suite name 不可靠（v17.136）

`--filter "Drawing 创建与类型契约"` 中文 suite 名匹配返回 0 tests。改用英文 struct name `--filter "DrawingFactoryTests"`（但也得对应实际类名）· 或直接跑全量。

### ChartScene 加按钮 / hover preview / handleDrawingTap / displayName / suggestCategory 5 处

新 DrawingType 必须更新这 5 处 + Drawing.swift case + factory + Drawings test 4 处 + DrawingsOverlayView dispatch + impl + color = 总共 ~11 处。漏一处可能 enum exhaustiveness 编译失败 / 渲染没效果 / 测试漏覆盖。

---

## 12 · 启动新会话推荐（直接复制）

```text
项目：中国期货 Mac/iPad 原生工作台 · v17.138 · 自选管理深化收口 + fib 7 件套收口 + Workspace 导入导出 + 时间范围预设 · 7 commit 已 push

【必读交接文件】
/home/beelink/macos_tmp/macos_futures_trading_v1/会话交接-2026-05-12-v17.138-自选管理收口+fib7件套+Workspace导入导出+时间范围预设.md

【必读差距清单】
/home/beelink/macos_tmp/macos_futures_trading_v1/差距清单与后续路线-v17.52后.md

【一句话状态】
v17.132 → v17.138 完成 · 7 commit 全部已 push · 17 文件 +1727/-18 行 · 测试 2704 → 2739 全绿（+35 Linux 单测 + 10 macOS 单测）· 自选管理深化 batch I-VII 全收口（v17.129-135 共 7 batch）· fib 画线 7 件套全收口（28 种画线类型）· Workspace 用户预设可 export/import JSON · 主图时间范围预设 ⌘⌥1-6

【本 session 7 大功能】
- 🎯 v17.132 自选多标签 InstrumentTagStore + 标签筛选（batch IV · 9 单测）
- 🎯 v17.133 自选置顶合约 pinnedInstrumentIDs ≤ 3（batch V · 10 单测）
- 🎯 v17.134 自选合约别名 InstrumentAliasStore + CSV 扩到 8 列（batch VI · 9 单测）
- 🎯 v17.135 批量备注 BatchNoteEditSheet（batch VII 收口）
- 🎯 v17.136 fibonacciSpiral 黄金螺旋（fib 7 件套收口 · DrawingType 27 → 28）
- 🎯 v17.137 Workspace 用户预设 export/import JSON 文件（macOS 实跑 10 单测）
- 🎯 v17.138 主图时间范围预设 ⌘⌥1-6 = 1D/1W/1M/3M/6M/1Y（7 单测）

【Mac 切机状态】
- ⏳ 累积 ~368 commit（含 v17.132-138 + v17.118-131 + 之前 ~347）
- 🔴 重点验证（7 项）：
  · v17.132 标签 🏷️N 标识 + 标签筛选 toolbar + sheet 中文输入
  · v17.133 📌 橙色置顶标识 + 排序时 pinned 在顶 + 已满 3/3 disabled
  · v17.134 别名替代 ID 显示 + tooltip + CSV 8 列 Excel 中文正常
  · v17.135 多选批量备注 sheet + chip 列出受影响合约
  · v17.136 fibonacciSpiral 工具栏 tornado · 螺旋 5 段 fib 数标号
  · v17.137 NSSavePanel / NSOpenPanel · 导入 NSAlert 三选一
  · v17.138 ⌘⌥1-6 切时间范围 · HUD 显「时间范围：1W（N 根）」

【下一步强推 · 🥇】
- 🥇 主图 overlay 框架 + VWAP + Pivot + SuperTrend（一次扩框架 · 3 个常用指标 · 1.5d · trader 直接价格判断需求最高）
- 🥈 Ichimoku 副图（接现有 SubChartView 框架 · 0.5d）
- 🥉 全局快捷键速查表（21 窗口统一 ⌘/ help · 0.5d）
- 4️⃣ 自选管理收尾（标签批量管理 + archive · 0.6d 边际递减）
- 5️⃣ Mac 切机验收（累积 ~368 commit · 用户主动说才做）

【剩余推迟项】
- #6 Pane 自由拖拽切分（2-3d · 明确推迟）
- #11 A1.4 10w bars 性能 Mac 实测
- #12 A6.2 CloudKit M7+
- MoneyFlow / Position 真持仓数据（CTP 搁置）
- Stubs.swift 订阅 tab（Stage B · IAP）

【关键提醒】
- 7 commit 全部已 push origin · 无积压
- ReplayDriver flaky 偶发 · 重跑通过 · 不动
- DrawingType 现 28 种（v17.136 测试 27→28 同 commit 修复）· 下次加 case 必须同 commit 改 DrawingsTests 4 处期望
- MainAppTests SPM testTarget 测试 executableTarget · Linux 0 tests · macOS 实跑
- Watchlist 加字段必须 decodeIfPresent + 旧 JSON 单测兼容（v17.131/133 模板）
- #expect 不能直接调 mutating 方法（v17.133 第二次踩坑 · 先 let ok = ... 再 #expect(ok)）
- @MainActor 测试 Suite 必须同 @MainActor 标记（v17.137 ShellViewModel pattern）
- memory `feedback_战略不补` 中"fibonacciSpiral / 艾略特"等条目用户主动要求时优先做
- 独立 build path = /tmp/build_v1730_b1
- 测试节奏 memory 第 5 次反馈强化 · commit 前必输出"测试节奏自检"行
- batch 聚类节奏：连续 batch 间 fallout test failure 收口在当前 batch · 不另开 commit
- swift-testing filter 中文 suite 名匹配不可靠 · 用英文 struct name 或跑全量
- 不主动建议 Mac 切机 / 不把切机当 🥇（memory 自动连贯到底 + 累积 Mac 验收）

请先读交接文件 · 然后回报"已了解" + 自动开工 🥇 主图 overlay 框架 + VWAP（或用户指定的下一步）。
```
