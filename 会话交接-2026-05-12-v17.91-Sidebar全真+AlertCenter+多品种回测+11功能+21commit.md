# 会话交接 v17.80 → v17.91 · Sidebar 6 section 全真数据 + AlertCenter 3 通道 + 多品种回测 · 21 commit 已 push · 2026-05-12

## 1 · 项目极简（30 秒了解）

**做什么**：中国期货 Mac/iPad 原生专业工作台 · Stage A 不接 CTP 下单
**关键时点**：M6（2026-09 末）= Pro 订阅上线 = 生死节点
**本 session 核心**（共 **21 commit · 11 语义化版本（v17.81-91）+ 10 差距清单补 · 全部已 push**）：

接 v17.80 之后差距清单"还能做什么"轮询 · 推进 11 项功能 · 主轴 = **Sidebar 6 section 全真数据 + AlertCenter 3 通道全接通 macOS 系统 + D4 v3 多品种多周期回测引擎** · 测试 2657 → **2667 全绿**（+10 新测试 · ReplayDriver flaky 偶发已知）。

### 21 commit 全列表（时间正序）

| commit | 主题 | 闭环 |
|--------|------|------|
| `5c602e0` | **v17.81** · Workspace 用户自定义预设（UserWorkspacePreset Codable + saveActive/delete/rename · UserDefaults shell.v1.userPresets · ⌘K 接通） | Workspace |
| `94c922e` | v17.81 · 差距清单补 | 文档 |
| `3ef0d1a` | **v17.82** · Sidebar 异动接 AnomalyDetector 真扫描 + AlertCore SystemNoticeChannel + SoundChannel actor（UserNotifications + NSSound · macOS-only #if） | Sidebar 真+AlertCenter |
| `f6d6df9` | v17.82 · 差距清单补 | 文档 |
| `97acf2c` | **v17.83** · D4 v3 多品种多周期组合回测引擎 + MultiAssetBacktestSheet 5×4=20 cells 矩阵 · 染色按 \|PnL\|/max · positiveRate 鲁棒性阈值 ≥70%/50-70%/<50% · 7 单测 | 回测 |
| `a59a1ff` | v17.83 · 差距清单补 | 文档 |
| `79aaadb` | **v17.84** · AlertCore InAppOverlayChannel actor + ShellWindow 顶部右侧 banner overlay（红橙渐变 · 5s 自动消失 · 点击切预警 + filter symbol · 3 单测） | AlertCenter |
| `bd873ae` | v17.84 · 差距清单补 | 文档 |
| `d6a73c1` | **v17.85** · 用户预设拖拽排序 + 双击重命名（UserPresetRef Transferable · TapGesture count:2 → renameSheet · 12 emoji 选择器） | Workspace polish |
| `50ebd05` | **v17.86** · 预警声音选择器（SoundChannel 14 静态 sounds + UserDefaults · AlertSoundPickerSheet 单选+试听+保存 · AlertWindow header 🔈 入口） | AlertCenter polish |
| `bf96225` | v17.85/86 · 差距清单补 | 文档 |
| `6294561` | **v17.87** · WorkspaceTab 拖拽排序（升级 v17.74 右键 ← → · WorkspaceTabRef Transferable · moveWorkspace(toLocalIndex:) remove+insert） | Workspace polish |
| `c146938` | v17.87 · 差距清单补 | 文档 |
| `3b1b3a9` | **v17.88** · Sidebar 异动 Timer 30s 实时刷新（@State cache + Timer.publish autoconnect · header "刚刚 / 30s 前 / 2m 前" 时间戳） | Sidebar 活着 |
| `15b4861` | **v17.89** · Sidebar 自选 mini 接 WatchlistBook 真自选（refreshWatchlistAsync · groups.first prefix 5 · effectiveWatchlist 真优先 fallback mock · 行点击切主图 · "真" 绿 chip） | Sidebar 真 |
| `a4a8639` | v17.88/89 · 差距清单补 | 文档 |
| `85c5387` | **v17.90** · Sidebar 板块 mini 接 SectorPresets 真数据（11 板块 avg changePct + \|Δ\| desc 取前 6 · Sector.icon · openWindow(sector)） | Sidebar 真 |
| `fcd2080` | v17.90 · 差距清单补 | 文档 |
| `7416102` | **v17.91** · Sidebar Position + Training section 行点击交互（position 切主图 · training 切 PrimaryTab · 全 6 section 闭环） | Sidebar 收尾 |
| `65c69cd` | v17.91 · 差距清单补 | 文档 |

**累计**：**19 文件变更 · +2220/-149 行 · 9 新文件 · 10 新测试 case · 测试 2667 全绿**。

---

## 2 · 仓库与环境

| 维度 | 值 |
|------|---|
| Linux 主目录 | `/home/beelink/macos_tmp/macos_futures_trading_v1` |
| Mac 主目录 | `/Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1` |
| 最新 commit | `65c69cd`（v17.91 差距清单补） |
| origin/main | **已同步 · 0 commits ahead**（本 session 21 次 push 全部成功） |
| 当前分支 | `main` |
| Linux 测试 | **2667 / 415 swift testing 全绿** · 性能 baseline pass |
| Mac 实测 | **本 session 未切机** · 累积 **~319 commit 待验**（v15.96 + v16.5 + v16.27 + v17.x → v17.91） |
| Mac 编译 | ⏳ 未验 |
| SSH 免密 | Mac → `beelink@vvsvr` 已配 |
| 独立 build path | `/tmp/build_v1730_b1`（避免主 .build 抢锁） |

---

## 3 · 新会话开工指引

### 3.1 Linux 端

```bash
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1   # 增量 1-5s
swift test  --build-path /tmp/build_v1730_b1   # 期待 2667 全绿（~14s · ReplayDriver flaky 偶发重跑通过）
```

### 3.2 Mac 切机（累积 ~319 commit 待验）

```bash
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                       # 拉本 session 21 commit + 之前累积
./mac_acceptance.sh --skip-shots               # ~1min 快速验
./mac_acceptance.sh                            # ~5min 完整验
```

---

## 4 · 本 session 关键产出（按功能闭环分组）

### 闭环 A · Sidebar 6 section 全真数据 + 行点击切窗口（v17.82 / 88 / 89 / 90 / 91）

至此 sidebar 全 6 section 接通真数据 + 行点击交互闭环：

| section | 数据源 | 行点击 | 完成 |
|---------|--------|--------|------|
| watchlist | SQLiteWatchlistBookStore（接续 v17.77 ⌘K 同源）+ fallback mock | openWindow(chart) + watchlistInstrumentSelected | v17.89 |
| sector | SectorPresets 11 板块 avg changePct 取前 6 | openWindow(sector) | v17.90 |
| position | SimulatedTradingStore（v17.21 已接） | openWindow(chart) + watchlistInstrumentSelected · 无持仓 fallback openWindow(trading) | v17.91 |
| anomaly | AnomalyDetector.scan() top 5 + Timer 30s 刷新 + 时间戳 | openWindow(chart) + watchlistInstrumentSelected | v17.82 + v17.88 |
| training | TrainingLogPersistence（v17.3 已接） | primaryTab = .training | v17.91 |
| alertHistory | SQLiteAlertHistoryStore（v17.70 已接） | openWindow(chart) + watchlistInstrumentSelected | v17.82 |

**视觉**：watchlist + sector header 加 "真" 绿色 chip · anomaly header 加扫描时间戳 "刚刚 / 30s 前 / 2m 前"。

### 闭环 B · AlertCenter 3 通道全接通 macOS 系统（v17.82 / 84 / 86）

| Channel | 实装 | 入口 / 视觉 | 完成 |
|---------|------|------------|------|
| `.systemNotice` | SystemNoticeChannel actor · UserNotifications · 首次 send 时 requestAuthorization · 失败 silent | macOS 通知中心 banner | v17.82 |
| `.sound` | SoundChannel actor · NSSound MainActor.run · 14 静态 sounds + UserDefaults | AlertWindow header 🔈 按钮 → AlertSoundPickerSheet 试听+保存 | v17.82 + v17.86 |
| `.inApp` | InAppOverlayChannel actor · NotificationCenter.post(.alertInAppOverlay) · 解耦 Linux 可跑性 | ShellWindow 顶部右侧 banner overlay · 红橙渐变 + alertName + symbol chip + message + 5s 自动消失 + ❌ 手动关闭 + 点击切 AlertWindow + filter symbol | v17.84 |

`AlertWindow.registerChannels` switch 化 · 5 channel 各自接通：inApp + systemNotice + sound 全真 · console / file / webhook 仍 Logging 占位。

新测试 +3：InAppOverlayChannelTests（kind / send event / 默认 notification name）。

### 闭环 C · D4 v3 多品种多周期组合回测（v17.83）

**引擎层**（IndicatorCore/FormulaEngine/Backtest/MultiAssetMultiPeriodBacktest.swift · ~180 行）：
- 接收 `[BacktestCell(symbol, periodLabel, bars)]` · 每 cell 独立调 SimpleBacktestEngine.run
- 单 cell 失败静默跳过 · 不阻塞其他
- `RobustnessReport`：cellCount / positiveCellCount / **positiveRate**（trader 判鲁棒性核心）/ avgPnL / avgSharpe / avgWinRate / totalTrades / best / worst
- 跨 cell PnL desc 排序

**UI**（MainApp/MultiAssetBacktestSheet.swift · ~310 行）：
- 5 标的 (rb / i / au / ag / cu) × 4 周期 (5m / 15m / 1H / D) = **20 cells 矩阵**
- 每 cell seed = `hash(symbol+period)` % 9999 · 派生独立 mock 轨迹（与 MonteCarloSheet 同 barsForSeed closure）
- 染色按 `|PnL| / global max` · 正绿负红 · 边框 + cornerRadius 4
- 解读 HUD：positiveRate ≥70% ✅ 鲁棒 / 50-70% ⚠️ 中等 / <50% ❌ 过拟合风险
- BacktestWindow 🧭 按钮入口（与 🎲 鲁棒性测试并列）

**测试 +7**：empty / 单 cell uptrend / 全 uptrend rate=1.0 / 混合 rate=0.5 / 空 bars 跳过 / totalTrades 累加 / avgWinRate。

### 闭环 D · Workspace 用户自定义预设（v17.81 / 85 / 87）

**v17.81 数据层**：
- 新 `UserWorkspacePreset` struct（Codable + Sendable · name + emoji + primaryTab + paneLayout + panes + createdAt）
- ShellViewModel.userPresets + `saveActiveWorkspaceAsUserPreset(name:emoji:)` + `deleteUserPreset(_:)` + `newWorkspace(from:UserWorkspacePreset)` + UserDefaults `shell.v1.userPresets`

**v17.81 UI**：
- WorkspacePresetPickerSheet 分两段（内置 5 / 我的预设 N）· 顶部「💾 保存当前为预设」按钮 + 输入 sheet（12 emoji 选择器）· 用户卡片右上角 trash 删除
- ⌘K 命令面板加用户预设命令（`新建我的预设：xxx`）+「💾 保存当前 Workspace 为预设…」

**v17.85 polish**：
- `moveUserPreset(_:toLocalIndex:)` · `renameUserPreset(_:to:emoji:)`
- userCard 双击（TapGesture count:2）→ renameSheet
- userCard `.draggable(UserPresetRef) + .dropDestination` · UserPresetRef Codable + Transferable

**v17.87 polish**：
- WorkspaceTab 拖拽排序（升级 v17.74 右键 ← →）
- `moveWorkspace(_:toLocalIndex:)` remove+insert · 自补偿 src<dst 时 dst-1
- WorkspaceTabRef Transferable · `.draggable + .dropDestination`

### 闭环 E · 各类 polish

| polish | 完成 |
|-------|------|
| AlertSoundPickerSheet 14 sound 单选 + 试听 + 保存（NSSound + UserDefaults） | v17.86 |
| Sidebar 异动 Timer 30s autoconnect + anomalyTimeAgo 时间戳分段 helper | v17.88 |
| Sidebar fallback mock helper（hash 派生稳定 price/change · 避免 view re-eval 抖动） | v17.89 |

---

## 5 · Mac 切机重点验证清单

累积 **~319 commit 待验**（v15.96 + v16.5 + v16.27 + v17.x → v17.91）

### 🔴 高优先（v17.81-91 新功能 · 影响交互 / 视觉）

**Sidebar 6 section 全真**（v17.82 / 88 / 89 / 90 / 91）：
- watchlist：trader 创建过 WatchlistBook group 后 · sidebar 自选显示真合约（header "真" 绿 chip）· 行点击切主图
- sector：11 板块按 \|avg Δ%\| desc 显示前 6 · Sector.icon · 行点击 → SectorWindow
- anomaly：30s 自动刷新 · header "刚刚 / 30s 前 / 2m 前" 时间戳变化（trader 看"活着"）
- position：行点击切主图 K 线（看该持仓品种） · 无持仓时显 "无持仓 · 打开模拟交易→"
- training：整 section 点击 → primaryTab=.training · 与 ⌘5 同效
- alertHistory：行点击切主图 + filter symbol

**AlertCenter 通道**（v17.82 / 84 / 86）：
- 触发任一 alert → **同时**：macOS 通知中心 banner（首次需授权）+ Funk 声响（默认）+ ShellWindow 顶部右侧 红橙 banner
- 红橙 banner 5s 自动消失 / ❌ 手动关闭 / 点击 → AlertWindow 打开 + 自动筛该 symbol
- AlertWindow header 🔈 按钮 → 14 sound 列表 · 点击试听 · 选中后保存 + 下次预警生效
- AlertWindow registerChannels switch 化 · 5 channel 全真接通（systemNotice/sound/inApp/console+file+webhook 占位）

**多品种多周期回测**（v17.83）：
- BacktestWindow 🧭 按钮 → MultiAssetBacktestSheet
- 5×4=20 cells 矩阵 · 染色按 \|PnL\| 强度 · 正绿负红
- 鲁棒性 HUD positiveRate 显色阈值 ≥70%/50-70%/<50%
- 点击试用 ▶ 跑批 · 检查 ~1s 完成 + outcomes 正确填充

**用户预设 + 拖拽**（v17.81 / 85 / 87）：
- WorkspaceTabBar + 按钮 → "💾 保存当前为预设" → 输入 name + 选 emoji → 保存
- PickerSheet 我的预设 section 显示用户保存
- 双击用户预设卡片 → 弹 rename sheet（含 emoji 选择器）
- 拖拽用户预设卡片重排 · 紫底 emoji+name 拖拽预览
- 拖拽 WorkspaceTab tab 重排（同 PrimaryTab 内）· accent 底色拖拽预览
- ⌘K 命令面板搜 "保存" / "新建我的预设" / 用户预设名都能匹配

### 🟡 中优先（Linux 编译跳过的 macOS API 风险）

- `UNUserNotificationCenter.current().requestAuthorization` 沙箱内权限 prompt（v17.82 首次触发 alert 时弹）
- `NSSound(named:)` macOS 内置 14 sound 实际可播放性（v17.86 试听按钮）
- `NSSound.play` MainActor.run 调用
- ShellWindow `.overlay(alignment: .topTrailing)` + `.transition(.move + .opacity)` 渐变 banner 动画（v17.84）
- WorkspacePresetPickerSheet `.simultaneousGesture(TapGesture(count: 2))` 双击触发（v17.85）
- `.draggable + .dropDestination` macOS 13+ Transferable 拖拽（v17.85 / 87）
- `Timer.publish(every: 30, on: .main, in: .common).autoconnect()` 主线程 tick（v17.88）

### 🟢 低优先（不影响功能）

- banner 红橙渐变细节
- 矩阵 cell 染色强度梯度
- emoji 选择器 12 个选项排版
- "真" / "30s 前" 时间戳字号视觉

---

## 6 · 累积变更风险评估

| 维度 | 状态 |
|------|---|
| commit 数量 | ~319 commit 待 Mac 验（v15.96 + v16.5 + v16.27 + v17.x → v17.91） |
| 文件覆盖 | 本 session 19 文件 · 9 新文件 · ShellSidebar 改动密集（5 个 commit 涉及） |
| 风险面 | ShellSidebar 5 commit 改动累积 + AlertCore 3 新 channel + 跨 module 引入（ShellWindow import AlertCore）· Mac 切机若 build 失败用 git bisect 范围 v17.81-91 定位 |
| 已知 flaky | `ReplayDriver "跑到末尾自动停"` 偶发失败 · 重跑通过 · 不动 |
| 已知 Linux 编译盲区 | UNUserNotificationCenter 权限 prompt / NSSound 实际播放 / banner 动画 / 拖拽 Transferable macOS 13+ |

---

## 7 · 剩余推迟项（不变）

| 项 | 工作量 | 状态 |
|---|---|---|
| #6 Pane 自由拖拽切分（custom layout） | 2-3d | **明确推迟**（差距清单 §3.2） |
| #11 A1.4 Hollow/Bars OHLC 10w bars 性能 | Mac 验证 | 需 Mac 实测 · Linux 无法测 |
| #12 A6.2 CloudKit CKContainer 真接 | M7+ | 需 Apple 设备 |
| MoneyFlow / Position Window 真持仓数据 | CTP 接入后 | CTP 用户已搁置 |
| Stubs.swift 数据 + 订阅 settings tab | Stage B | 用户搁置（CTP / IAP） |

---

## 8 · 下一会话计划 · 已确定 🥇 + 🥈

### 🥇 Settings 图表 tab 实装（0.5-1d）

**现状**：`Sources/MainApp/Stubs.swift:23` placeholder："颜色 / 指标 / 精度细化待补"

**实装**：
- 新建 `ChartSettingsTab: View` struct · 替换 placeholder
- 默认指标参数：MA 周期默认（5/10/20）/ MACD 默认（12/26/9）/ KDJ 默认（9/3/3）/ RSI 默认 14 / BOLL 默认（20, 2）
- K 线配色细化：红涨 / 绿跌默认（用户可切色）· 上影下影颜色 · MA 线默认颜色
- 价格精度：保留 N 位小数（trader 看不同合约调精度）
- @AppStorage 持久化（与 chart.v1.* 同 key 命名）
- 形式与 NotificationSettingsTab / PrivacySettingsTab 一致 · Form + Section + footer

**切入点**：
- 替换 `Stubs.swift:23` `placeholder(title: "图表", note: ...)` 为 `ChartSettingsTab()`
- 加 `private struct ChartSettingsTab: View { ... }`
- @AppStorage keys 命名建议：
  - `chart.default.ma.period1` = 5
  - `chart.default.ma.period2` = 10
  - `chart.default.ma.period3` = 20
  - `chart.default.macd.fast` = 12 / .slow = 26 / .signal = 9
  - `chart.default.kdj.k` = 9 / .d = 3 / .j = 3
  - `chart.default.rsi.period` = 14
  - `chart.default.boll.period` = 20 / .multiplier = 2.0
  - `chart.color.bullCandle` = "red" / "green" / "auto"（默认 auto = 红涨）
  - `chart.precision.digits` = 2 / 3 / 4 / auto

**ChartScene 接通**：
- 加指标时读 @AppStorage 默认值替代 hardcoded 5/10/20 等
- 主图 candle 染色读 chart.color.bullCandle
- 价格 label 读 chart.precision.digits

### 🥈 WorkspaceTab paneCount badge（0.3d · 小步快跑）

**现状**：WorkspaceTabBar tab 名旁无视觉提示

**实装**：
- WorkspaceTabBar.workspaceTab 内 · ws.name Text 旁加小角标
- 显示：`paneLayout.emoji + paneCount`（如 ⊞ 4 / ▦ 6）
- 字号 9-10pt · 半透明 secondary 色 · padding 1-2pt · cornerRadius 2
- isActive 时显高对比 · 非 active 时显淡

**切入点**：
- `Sources/MainApp/Shell/WorkspaceTabBar.swift:74` workspaceTab 内 ws.name Text 后追加 badge HStack

---

## 9 · 一行命令速查

```bash
# Linux 主开发
cd /home/beelink/macos_tmp/macos_futures_trading_v1
swift build --build-path /tmp/build_v1730_b1     # 1-5s 增量
swift test  --build-path /tmp/build_v1730_b1     # 期待 2667 全绿

# Mac 切机
cd /Users/admin/Documents/MAC版本_期货交易终端/macos_futures_trading_v1
git pull                                          # 拉本 session 21 commit
./mac_acceptance.sh --skip-shots                  # ~1min
./mac_acceptance.sh                               # ~5min

# 看本 session 全 commit
git log --oneline b7a3b09..HEAD                  # 21 commit
git diff b7a3b09..HEAD --stat                    # 19 文件 +2220/-149
```

---

## 10 · 启动新会话推荐（直接复制）

```text
项目：中国期货 Mac/iPad 原生工作台 · v17.91 · Shell 99.5% + Sidebar 6 section 全真 + AlertCenter 3 通道全接通 · 21 commit 已 push

【必读交接文件】
/home/beelink/macos_tmp/macos_futures_trading_v1/会话交接-2026-05-12-v17.91-Sidebar全真+AlertCenter+多品种回测+11功能+21commit.md

【必读差距清单】
/home/beelink/macos_tmp/macos_futures_trading_v1/差距清单与后续路线-v17.52后.md

【一句话状态】
v17.81 → v17.91 · 21 commit 全部已 push · +2220/-149 行 · 测试 2667 全绿（+10）· Sidebar 6 section 全真数据 + AlertCenter 3 通道接通 macOS 系统（systemNotice/sound/inAppOverlay）

【本 session 5 大功能闭环】
- 🎯 Sidebar 6 section 全真数据 + 行点击切窗口（v17.82/88/89/90/91 · watchlist/sector/position/anomaly/training/alertHistory）
- 🎯 AlertCenter 3 通道接通 macOS 系统（v17.82/84/86 · UserNotifications + NSSound + ShellWindow banner overlay + 14 sound 选择器）
- 🎯 D4 v3 多品种多周期组合回测引擎（v17.83 · 5×4=20 cells 矩阵 + RobustnessReport positiveRate）
- 🎯 Workspace 用户自定义预设 + 拖拽 + 重命名（v17.81/85/87 · UserDefaults shell.v1.userPresets · 双击 rename · UserPresetRef + WorkspaceTabRef Transferable）
- 🎯 Sidebar 异动 Timer 30s 实时刷新 + 扫描时间戳（v17.88 · trader 看"系统活着"）

【Mac 切机状态】
- ⏳ 累积 ~319 commit（v15.96 + v16.5 + v16.27 + v17.x → v17.91）
- 🔴 重点验证：
  · Sidebar 6 section 真数据 + 行点击切窗口
  · 触发 alert 时三联：macOS 通知中心 + Funk 声响 + ShellWindow 红橙 banner
  · 多品种回测 5×4 矩阵 + positiveRate 染色
  · 用户预设 + 拖拽 + 双击重命名
  · WorkspaceTab 拖拽排序
  · Sidebar 异动 30s 自动刷新 + 时间戳变化

【下一步已确定 · 🥇 + 🥈 一气呵成】
- 🥇 Settings 图表 tab 实装（0.5-1d · Stubs.swift:23 placeholder 替换 · ChartSettingsTab 加默认 MA/MACD/KDJ/RSI/BOLL 参数 + K 线配色 + 价格精度 + @AppStorage 持久化）
- 🥈 WorkspaceTab paneCount badge（0.3d · WorkspaceTabBar.swift:74 tab 名旁加 paneLayout.emoji + paneCount 角标）

【剩余推迟项】
- #6 Pane 自由拖拽切分（2-3d · 明确推迟）
- #11 A1.4 10w bars 性能 Mac 实测
- #12 A6.2 CloudKit M7+
- MoneyFlow / Position 真持仓数据（CTP 搁置）
- Stubs.swift 数据 + 订阅 tab（Stage B）

【关键提醒】
- 21 commit 全部已 push origin · 无积压
- ReplayDriver "跑到末尾自动停" 偶发 flaky · 重跑通过 · 不动
- 跨 module 新增：ShellWindow import AlertCore（v17.84）· AlertCore 加 #if canImport(UserNotifications/AppKit) 隔离
- AlertWindow registerChannels switch 化 · 5 channel 各自接通（inApp/systemNotice/sound 真 + console/file/webhook 占位）
- Sidebar 全 6 section button 化 · 不再有静态 HStack 行
- 独立 build path = /tmp/build_v1730_b1

请先读交接文件 · 然后回报"已了解" + 自动开工 🥇 Settings 图表 tab 实装。
```
